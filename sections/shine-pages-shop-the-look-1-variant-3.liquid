{% comment %} Variables  {% endcomment %}
{% assign settings = section.settings %}
<style>
    {% comment %}
    Render heading & body font family
{% endcomment %}
{%- if settings.override_font != blank -%}
    {%- capture font_settings_list -%}
        {{ settings.heading_font | font_face: font_display: 'swap' }};;
        {{ settings.heading_font | font_modify: 'weight', 'bolder' | font_face: font_display: 'swap' }};;
        {{ settings.heading_font | font_modify: 'style', 'italic' | font_face: font_display: 'swap' }};;
        {{ settings.heading_font | font_modify: 'style', 'italic' | font_modify: 'weight', 'bolder' | font_face: font_display: 'swap' }}
        {{ settings.body_font | font_face: font_display: 'swap' }};;
        {{ settings.body_font | font_modify: 'weight', 'bolder' | font_face: font_display: 'swap' }};;
        {{ settings.body_font | font_modify: 'style', 'italic' | font_face: font_display: 'swap' }};;
        {{ settings.body_font | font_modify: 'style', 'italic' | font_modify: 'weight', 'bolder' | font_face: font_display: 'swap' }};;
        {{ settings.hightlight_font | font_face: font_display: 'swap' }};;
        {{ settings.hightlight_font | font_modify: 'weight', 'bolder' | font_face: font_display: 'swap' }};;
        {{ settings.hightlight_font | font_modify: 'style', 'italic' | font_face: font_display: 'swap' }};;
        {{ settings.hightlight_font | font_modify: 'style', 'italic' | font_modify: 'weight', 'bolder' | font_face: font_display: 'swap' }};;
    {%- endcapture -%}
    {%- assign font_array = font_settings_list | split: ';;' -%}
{% endif %}
{%- if settings.override_font != blank -%}
    {%- for font in font_array -%}
        {%- unless font contains 'error' -%}
            {{ font }}{%- endunless -%}
    {%- endfor -%}
    #sp-{{ section.id }} {
    --sp-heading-font-family: {{ settings.heading_font.family }},{{ settings.heading_font.fallback_families }};
    --sp-heading-font-weight: {{ settings.heading_font.weight }};
    --sp-heading-font-style: {{ settings.heading_font.style }};
    --sp-body-font-family: {{ settings.body_font.family }},{{ settings.body_font.fallback_families }};
    --sp-body-font-weight: {{ settings.body_font.weight }};
    --sp-body-font-style: {{ settings.body_font.style }};
    --sp-highlight-font-family: {{ settings.highlight_font.family }},{{ settings.highlight_font.fallback_families }};
    --sp-highlight-font-weight: {{ settings.highlight_font.weight }};
    --sp-highlight-font-style: {{ settings.highlight_font.style }};
    }
    {% if custom_selector != blank %}
        {{ custom_selector }} {
        --sp-heading-font-family: {{ settings.heading_font.family }},{{ settings.heading_font.fallback_families }};
        --sp-heading-font-weight: {{ settings.heading_font.weight }};
        --sp-heading-font-style: {{ settings.heading_font.style }};
        --sp-body-font-family: {{ settings.body_font.family }},{{ settings.body_font.fallback_families }};
        --sp-body-font-weight: {{ settings.body_font.weight }};
        --sp-body-font-style: {{ settings.body_font.style }};
        --sp-highlight-font-family: {{ settings.highlight_font.family }},{{ settings.highlight_font.fallback_families }};
        --sp-highlight-font-weight: {{ settings.highlight_font.weight }};
        --sp-highlight-font-style: {{ settings.highlight_font.style }};
        }
    {% endif %}
{% endif %}
    {% comment %}
    Render padding section, heading content max width
{% endcomment %}

    #sp-{{ section.id }} .sp-section {
    {% if settings.section_top_padding_mobile and  settings.section_bottom_padding_mobile %}
        --sp-section-top-padding: {{ settings.section_top_padding_mobile | divided_by: 10.0 | round: 0 }}rem;
        --sp-section-bottom-padding: {{ settings.section_bottom_padding_mobile | divided_by: 10.0 | round: 0 }}rem;
    {% else %}
        --sp-section-top-padding: {{ settings.section_top_padding | divided_by: 10.0 | times: 0.5 | round: 0 }}rem;
        --sp-section-bottom-padding: {{ settings.section_bottom_padding | divided_by: 10.0 | times: 0.5 | round: 0 }}rem;
    {% endif %}
        }

    @media (min-width: 768px) {
        #sp-{{ section.id }} .sp-section {
            --sp-section-top-padding: {{ settings.section_top_padding | divided_by: 10.0 | times: 0.7 | round: 0 }}rem;
            --sp-section-bottom-padding: {{ settings.section_bottom_padding | divided_by: 10.0 | times: 0.7 | round: 0 }}rem;
        }
    }

    @media (min-width: 1260px) {
        #sp-{{ section.id }} .sp-section {
            --sp-section-top-padding: {{ settings.section_top_padding | divided_by: 10.0 | round: 0 }}rem;
            --sp-section-bottom-padding: {{ settings.section_bottom_padding | divided_by: 10.0 | round: 0 }}rem;
        }
    }


    #sp-{{ section.id }} {
        --sp-shop-the-look-image-border-radius: {{ settings.image_border_radius }}px;
        --sp-shop-the-look-image-overlay-color: {{ settings.image_overlay_color | default: null }};
    }

    .sp-modal-{{ section.id }} {
        --sp-heading-font-family: {{ settings.heading_font.family }},{{ settings.heading_font.fallback_families }};
        --sp-heading-font-weight: {{ settings.heading_font.weight }};
        --sp-heading-font-style: {{ settings.heading_font.style }};
        --sp-body-font-family: {{ settings.body_font.family }},{{ settings.body_font.fallback_families }};
        --sp-body-font-weight: {{ settings.body_font.weight }};
        --sp-body-font-style: {{ settings.body_font.style }};
    }

    #sp-{{ section.id }} .sp-section {
        --sp-section-background-color: {{ settings.section_background_color | default: "transparent" }};
        --sp-section-background-gradient-color: {{ settings.section_background_gradient_color | default : null }};
        --sp-section-text-color: {{ settings.section_text_color | default:"#1e1e1e" }};
    }

    #sp-{{ section.id }} .sp-heading-wrapper {
        --sp-section-heading-width: 0rem;
        max-width: var(--sp-section-heading-width);
    }

    #sp-{{ section.id }} .sp-shop-the-look-image-inner{
        --sp-shop-the-look-image-border-width: {{ settings.image_border_size }}px;
        --sp-shop-the-look-image-border-color: {{ settings.image_border_color | default:"transparent" }};
    }

    .sp-modal-{{ section.id }} .sp-shop-the-look-content-carousel {
        --sp-section-background-color: {{ settings.section_background_color | default: "transparent" }};
        --sp-section-background-gradient-color: {{ settings.section_background_gradient_color | default : null }};
    }

    #sp-{{ section.id }} .sp-shop-the-look-item .sp-shop-the-look-image-wrapper .sp-shop-the-look-hotspots .sp-shop-the-look-hotspot-item {
        --sp-shop-the-look-hotspot-color: {{ settings.hotspot_color | default: "#FFF" }}
    }

    #sp-{{ section.id }} .sp-product-card{
--sp-image-aspect-ratio: {{ settings.product_card_image_ratio}};
--sp-card-border-radius: {{ settings.product_card_border_radius }}px;
--sp-product-card-badge-border-radius: {{ settings.product_card_badge_border_radius }}px;
--sp-card-border-width: {{ settings.product_card_border_size }}px;

--sp-card-background-color: {{ settings.product_card_background_color | default: "#FFF" }};
--sp-card-background-gradient-color: {{ settings.product_card_background_gradient_color | default: null }};
--sp-card-shadow-color: {{ settings.product_card_shadow_color | default: "transparent" }};
--sp-card-border-color: {{ settings.product_card_border_color | default: 'none' }};
--sp-card-heading-color: {{ settings.product_card_text_color | default: "#1e1e1e" }};
--sp-product-card-price-color: {{ settings.product_card_price_color | default: "#1e1e1e" }};
}

#sp-{{ section.id }} .sp-product-card .sp-product-card-rating .sp-rating-star {
--sp-product-card-rating-color: {{ settings.product_card_price_rating_color | default: "#1e1e1e" }};
}

#sp-{{ section.id }} .sp-product-card .sp-product-card-badge .sp-badge.sp-badge--on-sale {
--sp-product-card-badge-background-color: {{ settings.product_card_discount_badge_background_color | default: "#FFF" }};
--sp-product-card-badge-text-color: {{ settings.product_card_discount_badge_text_color | default: "#1e1e1e" }};
}

#sp-{{ section.id }} .sp-product-card .sp-product-card-badge .sp-badge.sp-badge--sold-out {
--sp-product-card-badge-background-color: {{ settings.product_card_sold_badge_background_color | default: "#FFF" }};
--sp-product-card-badge-text-color: {{ settings.product_card_sold_badge_text_color | default: "#1e1e1e" }};
}

    .sp-modal-{{ section.id }} .sp-product-card {
        --sp-image-aspect-ratio: {{ settings.product_card_image_ratio}};
        --sp-card-border-radius: {{ settings.product_card_border_radius }}px;
        --sp-product-card-badge-border-radius: {{ settings.product_card_badge_border_radius }}px;
        --sp-card-border-width: {{ settings.product_card_border_size }}px;

        --sp-card-background-color: {{ settings.product_card_background_color | default: "#FFF" }};
        --sp-card-background-gradient-color: {{ settings.product_card_background_gradient_color | default: null }};
        --sp-card-shadow-color: {{ settings.product_card_shadow_color | default: "transparent" }};
        --sp-card-border-color: {{ settings.product_card_border_color | default: 'none' }};
        --sp-card-heading-color: {{ settings.product_card_text_color | default: "#1e1e1e" }};
        --sp-product-card-price-color: {{ settings.product_card_price_color | default: "#1e1e1e" }};
    }

    .sp-modal-{{ section.id }} .sp-product-card .sp-product-card-rating .sp-rating-star{
        --sp-product-card-rating-color: {{ settings.product_card_price_rating_color | default: "#1e1e1e" }};
    }

    .sp-modal-{{ section.id }} .sp-product-card .sp-product-card-badge .sp-badge.sp-badge--on-sale {
        --sp-product-card-badge-background-color: {{ settings.product_card_discount_badge_background_color | default: "#FFF" }};
        --sp-product-card-badge-text-color: {{ settings.product_card_discount_badge_text_color | default: "#1e1e1e" }};
    }

    .sp-modal-{{ section.id }} .sp-product-card .sp-product-card-badge .sp-badge.sp-badge--sold-out {
        --sp-product-card-badge-background-color: {{ settings.product_card_sold_badge_background_color | default: "#FFF" }};
        --sp-product-card-badge-text-color: {{ settings.product_card_sold_badge_text_color | default: "#1e1e1e" }};
    }

    #sp-{{ section.id }} [data-plugin="sp-carousel"],
    .sp-modal-{{ section.id }}[data-plugin="sp-carousel"]{
        --sp-slideshow-pagination-background-color: {{ settings.pagination_background | default: "transparent" }};
        --sp-slideshow-pagination-color: {{ settings.pagination_highlight | default: "transparent" }};
        --sp-slideshow-navigation-background-color: {{ settings.navigation_background | default: "transparent" }};
        --sp-slideshow-navigation-background-gradient-color: {{ settings.navigation_background_gradient | default: null }};
        --sp-slideshow-navigation-background-color: {{ settings.navigation_background | default: "transparent" }};
        --sp-slideshow-navigation-icon-color: {{ settings.navigation_icon | default: "transparent" }};
        --sp-navigation-border-width: {{ settings.navigation_button_border_width }}px;
        --sp-navigation-scale: {{ settings.navigation_button_scale | divided_by: 100.0 }};
        --sp-slideshow-navigarion-boxshadow-color: {{ settings.navigation_background_shadow }};
        --sp-slideshow-navigarion-border-radius: {% if settings.navigation_button_radius == 60 %}50%{% else %}{{ settings.navigation_button_radius }}px{% endif %};
    }

    .sp-modal-{{ section.id }} .sp-button-close {
        color: {{ settings.product_card_text_color | default:"#1e1e1e" }}
    }

</style>
<div id="sp-{{ section.id }}" class="sp-{{ section.id }} sp-section-shop-the-look-1" data-section="shop-the-look-1"
     data-variant="variant-3">
    <div class="sp-section sp-section-overflow" data-border="{{ settings.section_border }}">
        <div class="sp-container">
            <div class="sp-section-inner" data-lazy="sp-{% if settings.lazy_load %}lazy-section{% endif %}"
                 data-cascade>
                {% assign heading_block = section.blocks | where: 'type', 'heading_item' %}
                {% for block in heading_block %}
                    {% assign setting = block.settings %}
                    <style>
                        {% assign heading_settings = block.settings %}
                        {% assign heading_selector = section.id | prepend:"#sp-" | append:" .sp-heading-wrapper" %}
                        
    {{ heading_selector }} {
        --sp-section-heading-size: {{ heading_settings.heading_size_mobile }}px;
        --sp-section-description-size: {{ heading_settings.description_size_mobile }}px;
        --sp-heading-letter-spacing: {{ heading_settings.text_heading_spacing | default: 0 }}px;
        --sp-heading-spacing-scale: {{ heading_settings.heading_spacing_scale | divided_by: 100.0 | times: 0.6 }};

    }
    @media (min-width: 768px) {
        {{ heading_selector }} {
            --sp-heading-spacing-scale: {{ heading_settings.heading_spacing_scale | divided_by: 100.0 | times: 0.8 }};
            --sp-section-heading-size: {{ heading_settings.heading_size_desktop | times: 0.8 }}px;
            --sp-section-description-size: max({{ heading_settings.description_size_desktop | times: 0.8 }}px, {{ heading_settings.description_size_mobile }}px);
        }
    }

    @media (min-width: 1200px) {
        {{ heading_selector }} {
            --sp-heading-spacing-scale: {{ heading_settings.heading_spacing_scale | divided_by: 100.0 }};
            --sp-section-heading-size: {{ heading_settings.heading_size_desktop }}px;
            --sp-section-description-size: {{ heading_settings.description_size_desktop }}px;
        }
    }

                    </style>
                    <div class="sp-heading-outer {{ setting.heading_position }}" {{ block.shopify_attributes }}>
                        <div class="sp-heading-wrapper"
                             style="--sp-section-heading-width: {{ setting.heading_max_width | divided_by: 10.0 }}rem">
                            {% if setting.sub_heading != blank %}
                                <h5 class="sp-sub-heading {{ setting.sub_heading_size | default: "sp-sub-heading-size-medium" }}">{{ setting.sub_heading }}</h5>
                            {% endif %}
                            {% if setting.heading != blank %}
                                <h2 class="sp-heading">{{ setting.heading }}</h2>
                            {% endif %}
                            {% if setting.description != blank and setting.description != '<p></p>' %}
                                <div class="sp-description">{{ setting.description }}</div>
                            {% endif %}
                        </div>
                    </div>
                {% endfor %}
                <div class="sp-shop-the-looks" data-plugin="sp-carousel"
                     data-pagination-style="{{ settings.pagination_style | default: "circle" }}"
                     data-item-box-shadow="{% if settings.product_card_box_shadow or settings.box_shadow %}true{% else %}false{% endif %}"
                     data-pagination-position="{{ settings.pagination_position }}"
                     data-pagination-background="{{ settings.show_background_pagination }}"
                     data-show-pagination-desktop="true"
                     data-show-pagination-mobile="false"
                     data-show-navigation-desktop="true"
                     data-show-navigation-mobile="false"
                     data-navigation-box-shadow="{{ settings.navigation_box_shadow }}"
                     data-navigation-style="{{ settings.navigation_style | default: "fill" }}"
                >
                    <div class="sp-carousel">
                        {% assign product_card_content_alignment = settings.product_card_content_alignment %}
                        {% assign product_card_style = settings.product_card_style %}
                        {% assign product_card_box_shadow = settings.product_card_box_shadow %}
                        {% assign product_card_show_second_image = settings.product_card_show_second_image %}
                        {% assign product_card_show_vendor = settings.product_card_show_vendor %}
                        {% assign product_card_show_price = settings.product_card_show_price %}
                        {% assign product_card_show_discount_badge = settings.product_card_show_discount_badge %}
                        {% assign product_card_show_sold_out_badge = settings.product_card_show_sold_out_badge %}
                        {% assign product_card_discount_badge_type = settings.product_card_discount_badge_type %}
                        {% assign product_card_show_currency_code = settings.product_card_show_currency_code %}
                        {% assign product_card_show_rating = settings.product_card_show_rating %}
                        {% assign product_card_show_color_swatches = settings.product_card_show_color_swatches %}
                        {% assign product_card_color_swatch_style = settings.product_card_color_swatch_style %}

                        {% assign use_first_variant_product = true %}
                        {% assign show_compare_at_price = true %}

                        {% assign show_tooltip_color_swatch = true %}
                        {% assign max_color_swatches = 5 %}

                        {% assign currency_code_enabled = true %}
                        {% assign placeholder_product = false %}

                        {% assign slideIndex = 0 %}
                        {% for block in section.blocks %}
                            {% if block.type == 'shop_the_look_item' %}
                                {% assign setting = block.settings %}
                                <div class="sp-carousel-item"
                                     data-slider-index="{{ slideIndex }}" {{ block.shopify_attributes }}>
                                    <div class="sp-shop-the-look-item"
                                         data-image-position="{{ settings.image_position }}"
                                         data-modal-id="sp-modal-shop-the-look-{{ block.id }}"
                                         data-section-id="{{ section.id }}"
                                         data-box-shadow="{{ settings.box_shadow }}">
                                        <div class="sp-shop-the-look-image-wrapper">
                                            <div class="sp-shop-the-look-image-inner">
                                                {% if setting.shop_the_look_image != null %}
                                                    <img class="sp-image"
                                                         src="{{ setting.shop_the_look_image | image_url: width: 1920 }}"
                                                         alt="{{ setting.shop_the_look_image.alt | default: "" }}">
                                                {% else %}
                                                    {{ 'image' | placeholder_svg_tag: "sp-placeholder-image-gray-100" }}
                                                {% endif %}
                                            </div>
                                            <div class="sp-shop-the-look-hotspots">
                                                {% if setting.product == null and setting.product_2 == null and setting.product_3 == null and setting.product_4 == null %}
                                                    {% assign product = null %}
                                                    <div class="sp-shop-the-look-hotspot-item"
                                                         onclick="spChangeHotSpot(this)"
                                                         data-hotspot-id="sp-shop-the-look-product-item-1-1-{{ block.id }}"
                                                         data-modal-id="sp-modal-shop-the-look-{{ block.id }}"
                                                         style="--sp-shop-the-look-hotspot-top: {{ setting.vertical_position }}%; --sp-shop-the-look-hotspot-left: {{ setting.horizontal_position }}%;"></div>
                                                    <div class="sp-shop-the-look-hotspot-item"
                                                         onclick="spChangeHotSpot(this)"
                                                         data-hotspot-id="sp-shop-the-look-product-item-1-2-{{ block.id }}"
                                                         data-modal-id="sp-modal-shop-the-look-{{ block.id }}"
                                                         style="--sp-shop-the-look-hotspot-top: {{ setting.vertical_position_2 }}%; --sp-shop-the-look-hotspot-left: {{ setting.horizontal_position_2 }}%;"></div>
                                                    <div class="sp-shop-the-look-hotspot-item"
                                                         onclick="spChangeHotSpot(this)"
                                                         data-hotspot-id="sp-shop-the-look-product-item-1-3-{{ block.id }}"
                                                         data-modal-id="sp-modal-shop-the-look-{{ block.id }}"
                                                         style="--sp-shop-the-look-hotspot-top: {{ setting.vertical_position_3 }}%; --sp-shop-the-look-hotspot-left: {{ setting.horizontal_position_3 }}%;"></div>
                                                {% else %}
                                                    {% if setting.product != null %}
                                                        <div class="sp-shop-the-look-hotspot-item"
                                                             onclick="spChangeHotSpot(this)"
                                                             data-hotspot-id="sp-shop-the-look-product-item-1-{{ setting.product.id }}-{{ block.id }}"
                                                             data-modal-id="sp-modal-shop-the-look-{{ block.id }}"
                                                             style="--sp-shop-the-look-hotspot-top: {{ setting.vertical_position }}%; --sp-shop-the-look-hotspot-left: {{ setting.horizontal_position }}%;"></div>
                                                    {% endif %}
                                                    {% if setting.product_2 != null %}
                                                        <div class="sp-shop-the-look-hotspot-item"
                                                             onclick="spChangeHotSpot(this)"
                                                             data-modal-id="sp-modal-shop-the-look-{{ block.id }}"
                                                             data-hotspot-id="sp-shop-the-look-product-item-2-{{ setting.product_2.id }}-{{ block.id }}"
                                                             style="--sp-shop-the-look-hotspot-top: {{ setting.vertical_position_2 }}%; --sp-shop-the-look-hotspot-left: {{ setting.horizontal_position_2 }}%;"></div>
                                                    {% endif %}
                                                    {% if setting.product_3 != null %}
                                                        <div class="sp-shop-the-look-hotspot-item"
                                                             onclick="spChangeHotSpot(this)"
                                                             data-modal-id="sp-modal-shop-the-look-{{ block.id }}"
                                                             data-hotspot-id="sp-shop-the-look-product-item-3-{{ setting.product_3.id }}-{{ block.id }}"
                                                             style="--sp-shop-the-look-hotspot-top: {{ setting.vertical_position_3 }}%; --sp-shop-the-look-hotspot-left: {{ setting.horizontal_position_3 }}%;"></div>
                                                    {% endif %}
                                                    {% if setting.product_4 != null %}
                                                        <div class="sp-shop-the-look-hotspot-item"
                                                             onclick="spChangeHotSpot(this)"
                                                             data-modal-id="sp-modal-shop-the-look-{{ block.id }}"
                                                             data-hotspot-id="sp-shop-the-look-product-item-4-{{ setting.product_4.id }}-{{ block.id }}"
                                                             style="--sp-shop-the-look-hotspot-top: {{ setting.vertical_position_4 }}%; --sp-shop-the-look-hotspot-left: {{ setting.horizontal_position_4 }}%;"></div>
                                                    {% endif %}
                                                {% endif %}
                                            </div>
                                        </div>
                                        <div class="sp-shop-the-look-content-wrapper">
                                            <div class="sp-shop-the-look-content-carousel">
                                                {% if setting.product == null and setting.product_2 == null and setting.product_3 == null and setting.product_4 == null %}
                                                    {% assign placeholder_product = true %}
                                                    {% assign product = null %}
                                                    {% for i in (1..3) %}
                                                        <div class="sp-shop-the-look-product-item"
                                                             data-hotspot-id="sp-shop-the-look-product-item-1-{{ i }}-{{ block.id }}">
                                                            {% if product and product != empty %}
    {% capture productData %}
        {% assign onSale = false %}
        {% assign inventory = 0 %}
        {
        "id":{{- product.id -}},
        "available":{{- product.available | default: false -}},
        "title":{{- product.title | json -}},
        "handle":{{- product.handle | json -}},
        "url":"{{- product.url -}}",
        "type":{{- product.type | json -}},
        "price":{{- product.price | default: 0 -}},
        "price_max":{{- product.price_max | default: 0 -}},
        "price_min":{{- product.price_min | default: 0 -}},
        "compare_at_price":{{- product.compare_at_price | default: 0 -}},
        "compare_at_price_max": {{- product.compare_at_price_max | default: 0 -}},
        "compare_at_price_min": {{- product.compare_at_price_min | default: 0 -}},
        "price_varies": {{- product.price_varies | default: false -}},
        "compare_at_price_varies": {{- product.compare_at_price_varies | default: false -}},
        "created_at":"{{- product.created_at | date: "%s%L" -}}",
        "published_at":"{{- product.published_at | date: "%s%L" -}}",
        "tags": [{% for tag in product.tags %}{%- unless forloop.first -%},{%- endunless -%}{{- tag | json -}}{% endfor %}],
        "vendor": {{- product.vendor | json -}},
        "featured_image":"{{- product.featured_image -}}",
        "featured_media":"{% if product.featured_media %}{{- product.featured_media | image_url: width: 450 -}}{% endif %}",
        "images":[
        {%- for image in product.images -%}{%- unless forloop.first -%},
        {%- endunless -%}{"id":{{- image.id -}},
            "src":{{- image.src | json -}}}
        {%- endfor -%}],
        "collections":{{- product.collections | map: 'id' | json -}},
        {%- assign selectedVariant = product.selected_or_first_available_variant -%}
        {% assign sale_amount = 0 %}
        {% assign sale_percent = 0 %}
        {% if selectedVariant.price and selectedVariant.compare_at_price %}
            {% assign sale_amount = selectedVariant.compare_at_price | minus: selectedVariant.price %}
            {% if sale_amount > 0 %}
                {% assign divisor = selectedVariant.compare_at_price | times: 1.0 %}
                {% assign sale_percent = selectedVariant.compare_at_price | minus: selectedVariant.price | divided_by: divisor | times: 100.0 | round %}
            {% endif %}
        {% endif %}
        "variant": {
        "id": "{{ selectedVariant.id }}",
        "title": {{- selectedVariant.title | json -}},
        "available": {{- selectedVariant.available | default: false -}},
        "inventory_management": "{{- selectedVariant.inventory_management -}}",
        "inventory_policy": "{{- selectedVariant.inventory_policy -}}",
        "inventory_quantity": {{- selectedVariant.inventory_quantity | default: 0 -}},
        "price": {{- selectedVariant.price | default: 0 -}},
        "compare_at_price": {{- selectedVariant.compare_at_price | default: 0 -}},
        "unit_price": "{{- selectedVariant.unit_price -}}",
        "weight": "{{- selectedVariant.weight -}}",
        "weight_in_unit": "{{- selectedVariant.weight_in_unit -}}",
        "weight_unit": "{{- selectedVariant.weight_unit -}}",
        "sale_amount": {{- sale_amount | money | json -}},
        "sale_percent": {{- sale_percent | json -}}
        },
        "variants":{
        {%- for variant in product.variants -%}
            {%- unless forloop.first -%},{%- endunless -%}
            {% if variant.compare_at_price > variant.price %}
                {% assign onSale = true %}
            {% endif %}
            {% if variant.available and variant.inventory_management != null and variant.inventory_management != '' %}
                {% if variant.inventory_policy == 'deny' and variant.inventory_quantity > 0 %}
                    {% assign inventory = inventory | plus: variant.inventory_quantity %}
                {% elsif variant.inventory_policy == 'continue' %}
                    {% assign inventory = -1 %}
                {% endif %}
            {% elsif variant.available and variant.inventory_management == null %}
                {% assign inventory = -1 %}
            {% endif %}
            {% assign sale_amount = 0 %}
            {% assign sale_percent = 0 %}
            {% if variant.price and variant.compare_at_price %}
                {% assign sale_amount = variant.compare_at_price | minus: variant.price %}
                {% if sale_amount > 0 %}
                    {% assign divisor = variant.compare_at_price | times: 1.0 %}
                    {% assign sale_percent = variant.compare_at_price | minus: variant.price | divided_by: divisor | times: 100.0 | round %}
                {% endif %}
            {% endif %}
            "{{- variant.id -}}": {
            "id":{{- variant.id -}},
            "title":{{- variant.title | json }},
            "name":{{- variant.name | json }},
            "price":{{- variant.price | default: 0 -}},
            "compare_at_price":{{- variant.compare_at_price | default: 0 -}},
            "available":{{- variant.available | default: false -}},
            "sku":{{- variant.sku | json -}},
            "weight":"{{- variant.weight -}}",
            "weight_unit": "{{- variant.weight_unit -}}",
            "inventory_management": "{{- variant.inventory_management -}}",
            "inventory_policy": "{{- variant.inventory_policy -}}",
            "inventory_quantity": {{- variant.inventory_quantity | default: 0 -}},
            "selected": {%- if variant.selected -%}  {{- variant.selected -}} {%- elsif product.selected_or_first_available_variant.id == variant.id -%} true  {%- else -%} false {%- endif -%},
            "sale_amount": {{- sale_amount | money | json -}},
            "sale_percent": {{- sale_percent | json -}}
            }
        {%- endfor -%}
        },
        "on_sale":{{- onSale -}},
        "inventory":{{- inventory -}},
        "options":{{- product.options_with_values | json -}},
        "has_only_default_variant": {{ product.has_only_default_variant | default: false }}
        }
    {% endcapture %}
    <div class="sp-card sp-product-card"
         data-style="{{ product_card_style | default: "style-1" }}"
         data-content-alignment="{{ product_card_content_alignment }}"
         data-box-shadow="{{ product_card_box_shadow }}"
    >
        <div data-app="shine-trust-app" data-product="{{- productData | base64_encode -}}"></div>
        <div class="sp-card-inner">
            <div class="sp-card-media sp-product-card-media {% if product.media[1] != null and product_card_show_second_image %}has-second-image{% endif %}">
                <div class="sp-product-card-badge" data-alignment="vertical">
                    {% if product_card_show_discount_badge and product.compare_at_price > product.price %}
                        {% assign save = product.compare_at_price | minus: product.price | money %}
                        {% if product_card_show_currency_code %}
                            {% assign save = product.compare_at_price | minus: product.price | money_with_currency %}
                        {% endif %}
                        {% if product_card_discount_badge_type == 'percent' %}
                            {% assign save = product.compare_at_price | minus: product.price | times: 1.0 | divided_by: product.compare_at_price | times: 100 | round: 0 | append: "%" %}
                        {% endif %}
                        <span class="sp-badge sp-badge--on-sale">{{ 'shinepages.product.sale_html' | t: price: save }}</span>
                    {% endif %}
                    {% if product_card_show_sold_out_badge and product.available == false %}
                        <span class="sp-badge sp-badge--sold-out">{{ 'shinepages.product.sold_out' | t }}</span>
                    {% endif %}
                </div>
                <a class="sp-link" href="{{ product.url }}">
                    <img srcset="
                  {%- if product.featured_media.width >= 165 -%}{{ product.featured_media | image_url: width: 165 }} 165w,{%- endif -%}
                  {%- if product.featured_media.width >= 360 -%}{{ product.featured_media | image_url: width: 360 }} 360w,{%- endif -%}
                  {%- if product.featured_media.width >= 533 -%}{{ product.featured_media | image_url: width: 533 }} 533w,{%- endif -%}
                  {%- if product.featured_media.width >= 720 -%}{{ product.featured_media | image_url: width: 720 }} 720w,{%- endif -%}
                  {%- if product.featured_media.width >= 940 -%}{{ product.featured_media | image_url: width: 940 }} 940w,{%- endif -%}
                  {%- if product.featured_media.width >= 1066 -%}{{ product.featured_media | image_url: width: 1066 }} 1066w,{%- endif -%}
                  {{ product.featured_media | image_url }} {{ product.featured_media.width }}w
                "
                         src="{{ product.featured_media | image_url: width: 550 }}"
                         sizes="(min-width: 1200px) 1200px, (min-width: 990px) 990px, (min-width: 750px) 750px, 650px)"
                         alt="{{ product.featured_media.alt | escape }}"
                         width="{{ product.featured_media.width }}"
                         height="{{ product.featured_media.height }}"
                         data-change-variant-image="{{ product.featured_media | image_url: width: 550 }}"
                         data-change-variant-image-set="
                  {%- if product.featured_media.width >= 165 -%}{{ product.featured_media | image_url: width: 165 }} 165w,{%- endif -%}
                  {%- if product.featured_media.width >= 360 -%}{{ product.featured_media | image_url: width: 360 }} 360w,{%- endif -%}
                  {%- if product.featured_media.width >= 533 -%}{{ product.featured_media | image_url: width: 533 }} 533w,{%- endif -%}
                  {%- if product.featured_media.width >= 720 -%}{{ product.featured_media | image_url: width: 720 }} 720w,{%- endif -%}
                  {%- if product.featured_media.width >= 940 -%}{{ product.featured_media | image_url: width: 940 }} 940w,{%- endif -%}
                  {%- if product.featured_media.width >= 1066 -%}{{ product.featured_media | image_url: width: 1066 }} 1066w,{%- endif -%}
                  {{ product.featured_media | image_url }} {{ product.featured_media.width }}w
                "
                    >
                    {%- if product.media[1] != null and product_card_show_second_image -%}
                        <img
                                srcset="
                    {%- if product.media[1].width >= 165 -%}{{ product.media[1] | image_url: width: 165 }} 165w,{%- endif -%}
                    {%- if product.media[1].width >= 360 -%}{{ product.media[1] | image_url: width: 360 }} 360w,{%- endif -%}
                    {%- if product.media[1].width >= 533 -%}{{ product.media[1] | image_url: width: 533 }} 533w,{%- endif -%}
                    {%- if product.media[1].width >= 720 -%}{{ product.media[1] | image_url: width: 720 }} 720w,{%- endif -%}
                    {%- if product.media[1].width >= 940 -%}{{ product.media[1] | image_url: width: 940 }} 940w,{%- endif -%}
                    {%- if product.media[1].width >= 1066 -%}{{ product.media[1] | image_url: width: 1066 }} 1066w,{%- endif -%}
                    {{ product.media[1] | image_url }} {{ product.media[1].width }}w
                  "
                                src="{{ product.media[1] | image_url: width: 1080 }}"
                                sizes="(min-width: {{ settings.page_width }}px) {{ settings.page_width | minus: 130 | divided_by: 4 }}px, (min-width: 990px) calc((100vw - 130px) / 4), (min-width: 750px) calc((100vw - 120px) / 3), calc((100vw - 35px) / 2)"
                                alt=""
                                width="{{ product.media[1].width }}"
                                height="{{ product.media[1].height }}"
                        >
                    {%- endif -%}
                </a>
            </div>
            <div class="sp-card-information sp-product-card-information sp-text-alignment-{{ text_alignment }}">
                {% if product_card_show_vendor %}
                    <div class="sp-product-card-vendor">{{ product.vendor }}</div>
                {% endif %}
                <h3 class="sp-product-card-heading">
                    <a class="sp-link" href="{{ product.url }}">{{ product.title | escape }}</a>
                </h3>
                {% if product_card_show_rating and product.metafields.reviews.rating.value != blank %}
    <div class="sp-product-card-rating">
        {% liquid
            assign rating_decimal = 0
            assign decimal = product.metafields.reviews.rating.value.rating | modulo: 1
            if decimal >= 0.3 and decimal <= 0.7
                assign rating_decimal = 0.5
            elsif decimal > 0.7
                assign rating_decimal = 1
            endif
        %}
        <div class="sp-rating" role="img">
            <span aria-hidden="true" class="sp-rating-star"
                  style="--sp-rating: {{ product.metafields.reviews.rating.value.rating | floor }}; --sp-rating-max: {{ product.metafields.reviews.rating.value.scale_max }}; --sp-rating-decimal: {{ rating_decimal }};"></span>
            {% if product_card_show_rating_number %}
                <span class="sp-rating-number">({{ product.metafields.reviews.rating.value.rating | floor }})</span>
                {% endif %}
        </div>
    </div>
{%- endif -%}
                {% comment %}
  Renders a list of product's price (regular, sale)

  Accepts:
  - product: {Object} Product Liquid object (optional)
  - use_variant: {Boolean} Renders selected or first variant price instead of overall product pricing (optional)
  - price_class: {String} Adds a price class to the price element (optional)
  - show_compare_at_price: {Boolean} Renders the compare at price if the product matches the condition (optional)
  Usage:
  {% render 'price', product: product %}
{% endcomment %}
{% if product_card_show_price %}
    {%- liquid
        if use_first_variant_product
            assign target = product.selected_or_first_available_variant
        else
            assign target = product
        endif

        assign compare_at_price = target.compare_at_price
        assign price = target.price | default: 1999
        assign available = target.available | default: false
        assign money_price = price | money
        if product_card_show_currency_code
            assign money_price = price | money_with_currency
        endif
    -%}

    <div class="sp-product-card-price
    {%- if available == false %} price-sold-out{% endif -%}
    {%- if compare_at_price > price and product.quantity_price_breaks_configured? != true %} price-on-sale{% endif -%}
    {%- if compare_at_price > price and product.quantity_price_breaks_configured? %} volume-pricing-sale-badge{% endif -%}
    {%- if product.price_varies == false and product.compare_at_price_varies %} price-no-compare{% endif -%}
  ">
        <span class="price-item price-item--sale price-item--last">
            {% if product_card_show_currency_code %}
                {{ price | money_with_currency }}
            {% else %}
                {{ price | money }}
            {% endif %}
        </span>
        {%- if show_compare_at_price and compare_at_price > 0 %}
            <span class="price-item price-item--regular">
            {% if product_card_show_currency_code %}
                {{ compare_at_price | money_with_currency }}
            {% else %}
                {{ compare_at_price | money }}
            {% endif %}
        </span>
        {%- endif -%}
    </div>
{% endif %}

                {% assign variants = product.variants %}
{% if product_card_show_color_swatches and variants %}
    {% assign colorSwatcheKey = shop.metaobjects['app--40489222145--shine_pages_settings']['shine_pages_settings'].colorSwatchKey.value | downcase %}
    {% assign colorSwatches = shop.metaobjects['app--40489222145--shine_pages_settings']['shine_pages_settings'].colorSwatches.value %}
    {% assign hasVariants = "" %}
    <div class="sp-product-color-swatches" data-alignment="{{ product_card_content_alignment }}"
         data-style="{{ product_card_color_swatch_style }}" data-has-change-image="1">
        {% assign setFirstVariant = false %}
        {% assign numItem = 0 %}
        {%- for option in product.options_with_values -%}
            {%- assign downcased_option = option.name | downcase -%}
            {%- if downcased_option == 'color' or downcased_option == 'colour' or downcased_option == 'couleur' or downcased_option == colorSwatcheKey -%}
                {%- assign variant_option = 'option' | append: forloop.index -%}

                {%- for value in option.values -%}
                    {%- assign downcased_value = value | downcase -%}
                    {%- capture color_id -%}{{ section.id }}-{{ product.id }}-{% increment color_index %}{%- endcapture -%}

                    {%- for variant in product.variants -%}
                        {%- if variant[variant_option] == value -%}
                            {% for color in colorSwatches %}
                                {% if value == color[0] %}
                                    {% assign color_check = color[0] | append: ',' %}
                                    {% unless hasVariants contains color_check %}
                                        {% assign numItem = numItem | plus: 1 %}
                                        {% assign hasVariants = hasVariants | append: color[0] | append: ',' %}
                                        {% if numItem <= max_color_swatches %}
                                            <div class="sp-product-color-swatch-item {% if setFirstVariant == false %}active{% endif %}"
                                                 data-plugin="sp-tooltip"
                                                 onclick="spProductCardColorSwatches(this)"
                                                    {% if variant.featured_image %}
                                                        data-featured-image="{{ variant.featured_image | image_url: width: 550 | default: '' }}"
                                                        data-featured-image-srcset="
                                                            {%- if variant.featured_image.width >= 165 -%}{{ variant.featured_image | image_url: width: 165 }} 165w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 360 -%}{{ variant.featured_image | image_url: width: 360 }} 360w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 533 -%}{{ variant.featured_image | image_url: width: 533 }} 533w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 720 -%}{{ variant.featured_image | image_url: width: 720 }} 720w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 940 -%}{{ variant.featured_image | image_url: width: 940 }} 940w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 1066 -%}{{ variant.featured_image | image_url: width: 1066 }} 1066w,{%- endif -%}
                                                            {{ variant.featured_image | image_url }} {{ variant.featured_image.width }}w"
                                                    {% endif %}
                                            >
                                                <div class="sp-product-color"
                                                     style="--sp-product-card-color-swatches-color: {{ color[1] }};">
                                                </div>
                                                {% if show_tooltip_color_swatch %}
                                                    <span class="sp-tooltip">{{ color[0] | strip }}</span>
                                                {% endif %}
                                            </div>
                                        {% endif %}
                                        {% assign setFirstVariant = true %}
                                    {% endunless %}
                                {% endif %}
                            {% endfor %}
                        {%- endif -%}
                    {%- endfor -%}
                {% endfor %}
            {% endif %}
        {%- endfor -%}
        {% if numItem > max_color_swatches %}
            <div class="sp-product-color-swatch-item color-none">
                <div class="sp-text">{{ numItem | minus: max_color_swatches | prepend: '+' }}</div>
            </div>
        {% endif %}
    </div>
{% endif %}
            </div>
        </div>
    </div>
{% else %}
    {% if placeholder_product %}
        <div class="sp-card sp-product-card"
             data-style="{{ product_card_style | default: "style-1" }}"
             data-content-alignment="{{ product_card_content_alignment }}"
             data-box-shadow="{{ product_card_box_shadow }}"
        >
            <div class="sp-card-inner">
                <div class="sp-card-media sp-product-card-media {% if product_card_show_second_image %}has-second-image{% endif %}">
                    <div class="sp-product-card-badge" data-alignment="vertical">
                        {% if product_card_show_sold_out_badge %}
                            <span class="sp-badge sp-badge--sold-out">{{ 'shinepages.product.sold_out' | t }}</span>
                        {% endif %}
                        {% if product_card_show_discount_badge %}
                            {% assign save = 100.00 | money %}
                            {% if currency_code_enabled %}
                                {% assign save = 100.00 | money_with_currency %}
                            {% endif %}
                            {% if product_card_discount_badge_type == 'percent' %}
                                {% assign save = "10%" %}
                            {% endif %}
                            <span class="sp-badge sp-badge--on-sale">{{ 'shinepages.product.sale_html' | t: price: save }}</span>
                        {% endif %}
                    </div>
                    <a href="#.">
                        {{ 'product-1' | placeholder_svg_tag: 'sp-placeholder-product-1-light' }}
                        {% if product_card_show_second_image %}
                            {{ 'product-2' | placeholder_svg_tag: 'sp-placeholder-product-1-light' }}
                        {% endif %}
                    </a>
                </div>
                <div class="sp-card-information sp-product-card-information">
                    {% if product_card_show_vendor %}
                        <div class="sp-product-card-vendor">shinepages</div>
                    {% endif %}
                    <h3 class="sp-product-card-heading">
                        <a href="#.">Example Product Title</a>
                    </h3>
                    {% if product_card_show_rating %}
                        {% if product_card_show_rating and product.metafields.reviews.rating.value != blank %}
    <div class="sp-product-card-rating">
        {% liquid
            assign rating_decimal = 0
            assign decimal = product.metafields.reviews.rating.value.rating | modulo: 1
            if decimal >= 0.3 and decimal <= 0.7
                assign rating_decimal = 0.5
            elsif decimal > 0.7
                assign rating_decimal = 1
            endif
        %}
        <div class="sp-rating" role="img">
            <span aria-hidden="true" class="sp-rating-star"
                  style="--sp-rating: {{ product.metafields.reviews.rating.value.rating | floor }}; --sp-rating-max: {{ product.metafields.reviews.rating.value.scale_max }}; --sp-rating-decimal: {{ rating_decimal }};"></span>
            {% if product_card_show_rating_number %}
                <span class="sp-rating-number">({{ product.metafields.reviews.rating.value.rating | floor }})</span>
                {% endif %}
        </div>
    </div>
{%- endif -%}
                    {% endif %}

                    {% assign use_variant = true %}
                    {% assign show_compare_at_price = true %}
                    {% assign currency_code_enabled = true %}
                    {% comment %}
  Renders a list of product's price (regular, sale)

  Accepts:
  - product: {Object} Product Liquid object (optional)
  - use_variant: {Boolean} Renders selected or first variant price instead of overall product pricing (optional)
  - price_class: {String} Adds a price class to the price element (optional)
  - show_compare_at_price: {Boolean} Renders the compare at price if the product matches the condition (optional)
  Usage:
  {% render 'price', product: product %}
{% endcomment %}
{% if product_card_show_price %}
    {%- liquid
        if use_first_variant_product
            assign target = product.selected_or_first_available_variant
        else
            assign target = product
        endif

        assign compare_at_price = target.compare_at_price
        assign price = target.price | default: 1999
        assign available = target.available | default: false
        assign money_price = price | money
        if product_card_show_currency_code
            assign money_price = price | money_with_currency
        endif
    -%}

    <div class="sp-product-card-price
    {%- if available == false %} price-sold-out{% endif -%}
    {%- if compare_at_price > price and product.quantity_price_breaks_configured? != true %} price-on-sale{% endif -%}
    {%- if compare_at_price > price and product.quantity_price_breaks_configured? %} volume-pricing-sale-badge{% endif -%}
    {%- if product.price_varies == false and product.compare_at_price_varies %} price-no-compare{% endif -%}
  ">
        <span class="price-item price-item--sale price-item--last">
            {% if product_card_show_currency_code %}
                {{ price | money_with_currency }}
            {% else %}
                {{ price | money }}
            {% endif %}
        </span>
        {%- if show_compare_at_price and compare_at_price > 0 %}
            <span class="price-item price-item--regular">
            {% if product_card_show_currency_code %}
                {{ compare_at_price | money_with_currency }}
            {% else %}
                {{ compare_at_price | money }}
            {% endif %}
        </span>
        {%- endif -%}
    </div>
{% endif %}


                    {% if product_card_show_color_swatches %}
                        {% assign variants = product.variants %}
{% if product_card_show_color_swatches and variants %}
    {% assign colorSwatcheKey = shop.metaobjects['app--40489222145--shine_pages_settings']['shine_pages_settings'].colorSwatchKey.value | downcase %}
    {% assign colorSwatches = shop.metaobjects['app--40489222145--shine_pages_settings']['shine_pages_settings'].colorSwatches.value %}
    {% assign hasVariants = "" %}
    <div class="sp-product-color-swatches" data-alignment="{{ product_card_content_alignment }}"
         data-style="{{ product_card_color_swatch_style }}" data-has-change-image="1">
        {% assign setFirstVariant = false %}
        {% assign numItem = 0 %}
        {%- for option in product.options_with_values -%}
            {%- assign downcased_option = option.name | downcase -%}
            {%- if downcased_option == 'color' or downcased_option == 'colour' or downcased_option == 'couleur' or downcased_option == colorSwatcheKey -%}
                {%- assign variant_option = 'option' | append: forloop.index -%}

                {%- for value in option.values -%}
                    {%- assign downcased_value = value | downcase -%}
                    {%- capture color_id -%}{{ section.id }}-{{ product.id }}-{% increment color_index %}{%- endcapture -%}

                    {%- for variant in product.variants -%}
                        {%- if variant[variant_option] == value -%}
                            {% for color in colorSwatches %}
                                {% if value == color[0] %}
                                    {% assign color_check = color[0] | append: ',' %}
                                    {% unless hasVariants contains color_check %}
                                        {% assign numItem = numItem | plus: 1 %}
                                        {% assign hasVariants = hasVariants | append: color[0] | append: ',' %}
                                        {% if numItem <= max_color_swatches %}
                                            <div class="sp-product-color-swatch-item {% if setFirstVariant == false %}active{% endif %}"
                                                 data-plugin="sp-tooltip"
                                                 onclick="spProductCardColorSwatches(this)"
                                                    {% if variant.featured_image %}
                                                        data-featured-image="{{ variant.featured_image | image_url: width: 550 | default: '' }}"
                                                        data-featured-image-srcset="
                                                            {%- if variant.featured_image.width >= 165 -%}{{ variant.featured_image | image_url: width: 165 }} 165w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 360 -%}{{ variant.featured_image | image_url: width: 360 }} 360w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 533 -%}{{ variant.featured_image | image_url: width: 533 }} 533w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 720 -%}{{ variant.featured_image | image_url: width: 720 }} 720w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 940 -%}{{ variant.featured_image | image_url: width: 940 }} 940w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 1066 -%}{{ variant.featured_image | image_url: width: 1066 }} 1066w,{%- endif -%}
                                                            {{ variant.featured_image | image_url }} {{ variant.featured_image.width }}w"
                                                    {% endif %}
                                            >
                                                <div class="sp-product-color"
                                                     style="--sp-product-card-color-swatches-color: {{ color[1] }};">
                                                </div>
                                                {% if show_tooltip_color_swatch %}
                                                    <span class="sp-tooltip">{{ color[0] | strip }}</span>
                                                {% endif %}
                                            </div>
                                        {% endif %}
                                        {% assign setFirstVariant = true %}
                                    {% endunless %}
                                {% endif %}
                            {% endfor %}
                        {%- endif -%}
                    {%- endfor -%}
                {% endfor %}
            {% endif %}
        {%- endfor -%}
        {% if numItem > max_color_swatches %}
            <div class="sp-product-color-swatch-item color-none">
                <div class="sp-text">{{ numItem | minus: max_color_swatches | prepend: '+' }}</div>
            </div>
        {% endif %}
    </div>
{% endif %}
                    {% endif %}
                </div>
            </div>
        </div>
    {% endif %}
{% endif %}
                                                        </div>
                                                    {% endfor %}
                                                {% else %}
                                                    {% if setting.product != null %}
                                                        {% assign product = setting.product %}
                                                        <div class="sp-shop-the-look-product-item"
                                                             data-hotspot-id="sp-shop-the-look-product-item-1-{{ product.id }}-{{ block.id }}">
                                                            {% if product and product != empty %}
    {% capture productData %}
        {% assign onSale = false %}
        {% assign inventory = 0 %}
        {
        "id":{{- product.id -}},
        "available":{{- product.available | default: false -}},
        "title":{{- product.title | json -}},
        "handle":{{- product.handle | json -}},
        "url":"{{- product.url -}}",
        "type":{{- product.type | json -}},
        "price":{{- product.price | default: 0 -}},
        "price_max":{{- product.price_max | default: 0 -}},
        "price_min":{{- product.price_min | default: 0 -}},
        "compare_at_price":{{- product.compare_at_price | default: 0 -}},
        "compare_at_price_max": {{- product.compare_at_price_max | default: 0 -}},
        "compare_at_price_min": {{- product.compare_at_price_min | default: 0 -}},
        "price_varies": {{- product.price_varies | default: false -}},
        "compare_at_price_varies": {{- product.compare_at_price_varies | default: false -}},
        "created_at":"{{- product.created_at | date: "%s%L" -}}",
        "published_at":"{{- product.published_at | date: "%s%L" -}}",
        "tags": [{% for tag in product.tags %}{%- unless forloop.first -%},{%- endunless -%}{{- tag | json -}}{% endfor %}],
        "vendor": {{- product.vendor | json -}},
        "featured_image":"{{- product.featured_image -}}",
        "featured_media":"{% if product.featured_media %}{{- product.featured_media | image_url: width: 450 -}}{% endif %}",
        "images":[
        {%- for image in product.images -%}{%- unless forloop.first -%},
        {%- endunless -%}{"id":{{- image.id -}},
            "src":{{- image.src | json -}}}
        {%- endfor -%}],
        "collections":{{- product.collections | map: 'id' | json -}},
        {%- assign selectedVariant = product.selected_or_first_available_variant -%}
        {% assign sale_amount = 0 %}
        {% assign sale_percent = 0 %}
        {% if selectedVariant.price and selectedVariant.compare_at_price %}
            {% assign sale_amount = selectedVariant.compare_at_price | minus: selectedVariant.price %}
            {% if sale_amount > 0 %}
                {% assign divisor = selectedVariant.compare_at_price | times: 1.0 %}
                {% assign sale_percent = selectedVariant.compare_at_price | minus: selectedVariant.price | divided_by: divisor | times: 100.0 | round %}
            {% endif %}
        {% endif %}
        "variant": {
        "id": "{{ selectedVariant.id }}",
        "title": {{- selectedVariant.title | json -}},
        "available": {{- selectedVariant.available | default: false -}},
        "inventory_management": "{{- selectedVariant.inventory_management -}}",
        "inventory_policy": "{{- selectedVariant.inventory_policy -}}",
        "inventory_quantity": {{- selectedVariant.inventory_quantity | default: 0 -}},
        "price": {{- selectedVariant.price | default: 0 -}},
        "compare_at_price": {{- selectedVariant.compare_at_price | default: 0 -}},
        "unit_price": "{{- selectedVariant.unit_price -}}",
        "weight": "{{- selectedVariant.weight -}}",
        "weight_in_unit": "{{- selectedVariant.weight_in_unit -}}",
        "weight_unit": "{{- selectedVariant.weight_unit -}}",
        "sale_amount": {{- sale_amount | money | json -}},
        "sale_percent": {{- sale_percent | json -}}
        },
        "variants":{
        {%- for variant in product.variants -%}
            {%- unless forloop.first -%},{%- endunless -%}
            {% if variant.compare_at_price > variant.price %}
                {% assign onSale = true %}
            {% endif %}
            {% if variant.available and variant.inventory_management != null and variant.inventory_management != '' %}
                {% if variant.inventory_policy == 'deny' and variant.inventory_quantity > 0 %}
                    {% assign inventory = inventory | plus: variant.inventory_quantity %}
                {% elsif variant.inventory_policy == 'continue' %}
                    {% assign inventory = -1 %}
                {% endif %}
            {% elsif variant.available and variant.inventory_management == null %}
                {% assign inventory = -1 %}
            {% endif %}
            {% assign sale_amount = 0 %}
            {% assign sale_percent = 0 %}
            {% if variant.price and variant.compare_at_price %}
                {% assign sale_amount = variant.compare_at_price | minus: variant.price %}
                {% if sale_amount > 0 %}
                    {% assign divisor = variant.compare_at_price | times: 1.0 %}
                    {% assign sale_percent = variant.compare_at_price | minus: variant.price | divided_by: divisor | times: 100.0 | round %}
                {% endif %}
            {% endif %}
            "{{- variant.id -}}": {
            "id":{{- variant.id -}},
            "title":{{- variant.title | json }},
            "name":{{- variant.name | json }},
            "price":{{- variant.price | default: 0 -}},
            "compare_at_price":{{- variant.compare_at_price | default: 0 -}},
            "available":{{- variant.available | default: false -}},
            "sku":{{- variant.sku | json -}},
            "weight":"{{- variant.weight -}}",
            "weight_unit": "{{- variant.weight_unit -}}",
            "inventory_management": "{{- variant.inventory_management -}}",
            "inventory_policy": "{{- variant.inventory_policy -}}",
            "inventory_quantity": {{- variant.inventory_quantity | default: 0 -}},
            "selected": {%- if variant.selected -%}  {{- variant.selected -}} {%- elsif product.selected_or_first_available_variant.id == variant.id -%} true  {%- else -%} false {%- endif -%},
            "sale_amount": {{- sale_amount | money | json -}},
            "sale_percent": {{- sale_percent | json -}}
            }
        {%- endfor -%}
        },
        "on_sale":{{- onSale -}},
        "inventory":{{- inventory -}},
        "options":{{- product.options_with_values | json -}},
        "has_only_default_variant": {{ product.has_only_default_variant | default: false }}
        }
    {% endcapture %}
    <div class="sp-card sp-product-card"
         data-style="{{ product_card_style | default: "style-1" }}"
         data-content-alignment="{{ product_card_content_alignment }}"
         data-box-shadow="{{ product_card_box_shadow }}"
    >
        <div data-app="shine-trust-app" data-product="{{- productData | base64_encode -}}"></div>
        <div class="sp-card-inner">
            <div class="sp-card-media sp-product-card-media {% if product.media[1] != null and product_card_show_second_image %}has-second-image{% endif %}">
                <div class="sp-product-card-badge" data-alignment="vertical">
                    {% if product_card_show_discount_badge and product.compare_at_price > product.price %}
                        {% assign save = product.compare_at_price | minus: product.price | money %}
                        {% if product_card_show_currency_code %}
                            {% assign save = product.compare_at_price | minus: product.price | money_with_currency %}
                        {% endif %}
                        {% if product_card_discount_badge_type == 'percent' %}
                            {% assign save = product.compare_at_price | minus: product.price | times: 1.0 | divided_by: product.compare_at_price | times: 100 | round: 0 | append: "%" %}
                        {% endif %}
                        <span class="sp-badge sp-badge--on-sale">{{ 'shinepages.product.sale_html' | t: price: save }}</span>
                    {% endif %}
                    {% if product_card_show_sold_out_badge and product.available == false %}
                        <span class="sp-badge sp-badge--sold-out">{{ 'shinepages.product.sold_out' | t }}</span>
                    {% endif %}
                </div>
                <a class="sp-link" href="{{ product.url }}">
                    <img srcset="
                  {%- if product.featured_media.width >= 165 -%}{{ product.featured_media | image_url: width: 165 }} 165w,{%- endif -%}
                  {%- if product.featured_media.width >= 360 -%}{{ product.featured_media | image_url: width: 360 }} 360w,{%- endif -%}
                  {%- if product.featured_media.width >= 533 -%}{{ product.featured_media | image_url: width: 533 }} 533w,{%- endif -%}
                  {%- if product.featured_media.width >= 720 -%}{{ product.featured_media | image_url: width: 720 }} 720w,{%- endif -%}
                  {%- if product.featured_media.width >= 940 -%}{{ product.featured_media | image_url: width: 940 }} 940w,{%- endif -%}
                  {%- if product.featured_media.width >= 1066 -%}{{ product.featured_media | image_url: width: 1066 }} 1066w,{%- endif -%}
                  {{ product.featured_media | image_url }} {{ product.featured_media.width }}w
                "
                         src="{{ product.featured_media | image_url: width: 550 }}"
                         sizes="(min-width: 1200px) 1200px, (min-width: 990px) 990px, (min-width: 750px) 750px, 650px)"
                         alt="{{ product.featured_media.alt | escape }}"
                         width="{{ product.featured_media.width }}"
                         height="{{ product.featured_media.height }}"
                         data-change-variant-image="{{ product.featured_media | image_url: width: 550 }}"
                         data-change-variant-image-set="
                  {%- if product.featured_media.width >= 165 -%}{{ product.featured_media | image_url: width: 165 }} 165w,{%- endif -%}
                  {%- if product.featured_media.width >= 360 -%}{{ product.featured_media | image_url: width: 360 }} 360w,{%- endif -%}
                  {%- if product.featured_media.width >= 533 -%}{{ product.featured_media | image_url: width: 533 }} 533w,{%- endif -%}
                  {%- if product.featured_media.width >= 720 -%}{{ product.featured_media | image_url: width: 720 }} 720w,{%- endif -%}
                  {%- if product.featured_media.width >= 940 -%}{{ product.featured_media | image_url: width: 940 }} 940w,{%- endif -%}
                  {%- if product.featured_media.width >= 1066 -%}{{ product.featured_media | image_url: width: 1066 }} 1066w,{%- endif -%}
                  {{ product.featured_media | image_url }} {{ product.featured_media.width }}w
                "
                    >
                    {%- if product.media[1] != null and product_card_show_second_image -%}
                        <img
                                srcset="
                    {%- if product.media[1].width >= 165 -%}{{ product.media[1] | image_url: width: 165 }} 165w,{%- endif -%}
                    {%- if product.media[1].width >= 360 -%}{{ product.media[1] | image_url: width: 360 }} 360w,{%- endif -%}
                    {%- if product.media[1].width >= 533 -%}{{ product.media[1] | image_url: width: 533 }} 533w,{%- endif -%}
                    {%- if product.media[1].width >= 720 -%}{{ product.media[1] | image_url: width: 720 }} 720w,{%- endif -%}
                    {%- if product.media[1].width >= 940 -%}{{ product.media[1] | image_url: width: 940 }} 940w,{%- endif -%}
                    {%- if product.media[1].width >= 1066 -%}{{ product.media[1] | image_url: width: 1066 }} 1066w,{%- endif -%}
                    {{ product.media[1] | image_url }} {{ product.media[1].width }}w
                  "
                                src="{{ product.media[1] | image_url: width: 1080 }}"
                                sizes="(min-width: {{ settings.page_width }}px) {{ settings.page_width | minus: 130 | divided_by: 4 }}px, (min-width: 990px) calc((100vw - 130px) / 4), (min-width: 750px) calc((100vw - 120px) / 3), calc((100vw - 35px) / 2)"
                                alt=""
                                width="{{ product.media[1].width }}"
                                height="{{ product.media[1].height }}"
                        >
                    {%- endif -%}
                </a>
            </div>
            <div class="sp-card-information sp-product-card-information sp-text-alignment-{{ text_alignment }}">
                {% if product_card_show_vendor %}
                    <div class="sp-product-card-vendor">{{ product.vendor }}</div>
                {% endif %}
                <h3 class="sp-product-card-heading">
                    <a class="sp-link" href="{{ product.url }}">{{ product.title | escape }}</a>
                </h3>
                {% if product_card_show_rating and product.metafields.reviews.rating.value != blank %}
    <div class="sp-product-card-rating">
        {% liquid
            assign rating_decimal = 0
            assign decimal = product.metafields.reviews.rating.value.rating | modulo: 1
            if decimal >= 0.3 and decimal <= 0.7
                assign rating_decimal = 0.5
            elsif decimal > 0.7
                assign rating_decimal = 1
            endif
        %}
        <div class="sp-rating" role="img">
            <span aria-hidden="true" class="sp-rating-star"
                  style="--sp-rating: {{ product.metafields.reviews.rating.value.rating | floor }}; --sp-rating-max: {{ product.metafields.reviews.rating.value.scale_max }}; --sp-rating-decimal: {{ rating_decimal }};"></span>
            {% if product_card_show_rating_number %}
                <span class="sp-rating-number">({{ product.metafields.reviews.rating.value.rating | floor }})</span>
                {% endif %}
        </div>
    </div>
{%- endif -%}
                {% comment %}
  Renders a list of product's price (regular, sale)

  Accepts:
  - product: {Object} Product Liquid object (optional)
  - use_variant: {Boolean} Renders selected or first variant price instead of overall product pricing (optional)
  - price_class: {String} Adds a price class to the price element (optional)
  - show_compare_at_price: {Boolean} Renders the compare at price if the product matches the condition (optional)
  Usage:
  {% render 'price', product: product %}
{% endcomment %}
{% if product_card_show_price %}
    {%- liquid
        if use_first_variant_product
            assign target = product.selected_or_first_available_variant
        else
            assign target = product
        endif

        assign compare_at_price = target.compare_at_price
        assign price = target.price | default: 1999
        assign available = target.available | default: false
        assign money_price = price | money
        if product_card_show_currency_code
            assign money_price = price | money_with_currency
        endif
    -%}

    <div class="sp-product-card-price
    {%- if available == false %} price-sold-out{% endif -%}
    {%- if compare_at_price > price and product.quantity_price_breaks_configured? != true %} price-on-sale{% endif -%}
    {%- if compare_at_price > price and product.quantity_price_breaks_configured? %} volume-pricing-sale-badge{% endif -%}
    {%- if product.price_varies == false and product.compare_at_price_varies %} price-no-compare{% endif -%}
  ">
        <span class="price-item price-item--sale price-item--last">
            {% if product_card_show_currency_code %}
                {{ price | money_with_currency }}
            {% else %}
                {{ price | money }}
            {% endif %}
        </span>
        {%- if show_compare_at_price and compare_at_price > 0 %}
            <span class="price-item price-item--regular">
            {% if product_card_show_currency_code %}
                {{ compare_at_price | money_with_currency }}
            {% else %}
                {{ compare_at_price | money }}
            {% endif %}
        </span>
        {%- endif -%}
    </div>
{% endif %}

                {% assign variants = product.variants %}
{% if product_card_show_color_swatches and variants %}
    {% assign colorSwatcheKey = shop.metaobjects['app--40489222145--shine_pages_settings']['shine_pages_settings'].colorSwatchKey.value | downcase %}
    {% assign colorSwatches = shop.metaobjects['app--40489222145--shine_pages_settings']['shine_pages_settings'].colorSwatches.value %}
    {% assign hasVariants = "" %}
    <div class="sp-product-color-swatches" data-alignment="{{ product_card_content_alignment }}"
         data-style="{{ product_card_color_swatch_style }}" data-has-change-image="1">
        {% assign setFirstVariant = false %}
        {% assign numItem = 0 %}
        {%- for option in product.options_with_values -%}
            {%- assign downcased_option = option.name | downcase -%}
            {%- if downcased_option == 'color' or downcased_option == 'colour' or downcased_option == 'couleur' or downcased_option == colorSwatcheKey -%}
                {%- assign variant_option = 'option' | append: forloop.index -%}

                {%- for value in option.values -%}
                    {%- assign downcased_value = value | downcase -%}
                    {%- capture color_id -%}{{ section.id }}-{{ product.id }}-{% increment color_index %}{%- endcapture -%}

                    {%- for variant in product.variants -%}
                        {%- if variant[variant_option] == value -%}
                            {% for color in colorSwatches %}
                                {% if value == color[0] %}
                                    {% assign color_check = color[0] | append: ',' %}
                                    {% unless hasVariants contains color_check %}
                                        {% assign numItem = numItem | plus: 1 %}
                                        {% assign hasVariants = hasVariants | append: color[0] | append: ',' %}
                                        {% if numItem <= max_color_swatches %}
                                            <div class="sp-product-color-swatch-item {% if setFirstVariant == false %}active{% endif %}"
                                                 data-plugin="sp-tooltip"
                                                 onclick="spProductCardColorSwatches(this)"
                                                    {% if variant.featured_image %}
                                                        data-featured-image="{{ variant.featured_image | image_url: width: 550 | default: '' }}"
                                                        data-featured-image-srcset="
                                                            {%- if variant.featured_image.width >= 165 -%}{{ variant.featured_image | image_url: width: 165 }} 165w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 360 -%}{{ variant.featured_image | image_url: width: 360 }} 360w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 533 -%}{{ variant.featured_image | image_url: width: 533 }} 533w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 720 -%}{{ variant.featured_image | image_url: width: 720 }} 720w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 940 -%}{{ variant.featured_image | image_url: width: 940 }} 940w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 1066 -%}{{ variant.featured_image | image_url: width: 1066 }} 1066w,{%- endif -%}
                                                            {{ variant.featured_image | image_url }} {{ variant.featured_image.width }}w"
                                                    {% endif %}
                                            >
                                                <div class="sp-product-color"
                                                     style="--sp-product-card-color-swatches-color: {{ color[1] }};">
                                                </div>
                                                {% if show_tooltip_color_swatch %}
                                                    <span class="sp-tooltip">{{ color[0] | strip }}</span>
                                                {% endif %}
                                            </div>
                                        {% endif %}
                                        {% assign setFirstVariant = true %}
                                    {% endunless %}
                                {% endif %}
                            {% endfor %}
                        {%- endif -%}
                    {%- endfor -%}
                {% endfor %}
            {% endif %}
        {%- endfor -%}
        {% if numItem > max_color_swatches %}
            <div class="sp-product-color-swatch-item color-none">
                <div class="sp-text">{{ numItem | minus: max_color_swatches | prepend: '+' }}</div>
            </div>
        {% endif %}
    </div>
{% endif %}
            </div>
        </div>
    </div>
{% else %}
    {% if placeholder_product %}
        <div class="sp-card sp-product-card"
             data-style="{{ product_card_style | default: "style-1" }}"
             data-content-alignment="{{ product_card_content_alignment }}"
             data-box-shadow="{{ product_card_box_shadow }}"
        >
            <div class="sp-card-inner">
                <div class="sp-card-media sp-product-card-media {% if product_card_show_second_image %}has-second-image{% endif %}">
                    <div class="sp-product-card-badge" data-alignment="vertical">
                        {% if product_card_show_sold_out_badge %}
                            <span class="sp-badge sp-badge--sold-out">{{ 'shinepages.product.sold_out' | t }}</span>
                        {% endif %}
                        {% if product_card_show_discount_badge %}
                            {% assign save = 100.00 | money %}
                            {% if currency_code_enabled %}
                                {% assign save = 100.00 | money_with_currency %}
                            {% endif %}
                            {% if product_card_discount_badge_type == 'percent' %}
                                {% assign save = "10%" %}
                            {% endif %}
                            <span class="sp-badge sp-badge--on-sale">{{ 'shinepages.product.sale_html' | t: price: save }}</span>
                        {% endif %}
                    </div>
                    <a href="#.">
                        {{ 'product-1' | placeholder_svg_tag: 'sp-placeholder-product-1-light' }}
                        {% if product_card_show_second_image %}
                            {{ 'product-2' | placeholder_svg_tag: 'sp-placeholder-product-1-light' }}
                        {% endif %}
                    </a>
                </div>
                <div class="sp-card-information sp-product-card-information">
                    {% if product_card_show_vendor %}
                        <div class="sp-product-card-vendor">shinepages</div>
                    {% endif %}
                    <h3 class="sp-product-card-heading">
                        <a href="#.">Example Product Title</a>
                    </h3>
                    {% if product_card_show_rating %}
                        {% if product_card_show_rating and product.metafields.reviews.rating.value != blank %}
    <div class="sp-product-card-rating">
        {% liquid
            assign rating_decimal = 0
            assign decimal = product.metafields.reviews.rating.value.rating | modulo: 1
            if decimal >= 0.3 and decimal <= 0.7
                assign rating_decimal = 0.5
            elsif decimal > 0.7
                assign rating_decimal = 1
            endif
        %}
        <div class="sp-rating" role="img">
            <span aria-hidden="true" class="sp-rating-star"
                  style="--sp-rating: {{ product.metafields.reviews.rating.value.rating | floor }}; --sp-rating-max: {{ product.metafields.reviews.rating.value.scale_max }}; --sp-rating-decimal: {{ rating_decimal }};"></span>
            {% if product_card_show_rating_number %}
                <span class="sp-rating-number">({{ product.metafields.reviews.rating.value.rating | floor }})</span>
                {% endif %}
        </div>
    </div>
{%- endif -%}
                    {% endif %}

                    {% assign use_variant = true %}
                    {% assign show_compare_at_price = true %}
                    {% assign currency_code_enabled = true %}
                    {% comment %}
  Renders a list of product's price (regular, sale)

  Accepts:
  - product: {Object} Product Liquid object (optional)
  - use_variant: {Boolean} Renders selected or first variant price instead of overall product pricing (optional)
  - price_class: {String} Adds a price class to the price element (optional)
  - show_compare_at_price: {Boolean} Renders the compare at price if the product matches the condition (optional)
  Usage:
  {% render 'price', product: product %}
{% endcomment %}
{% if product_card_show_price %}
    {%- liquid
        if use_first_variant_product
            assign target = product.selected_or_first_available_variant
        else
            assign target = product
        endif

        assign compare_at_price = target.compare_at_price
        assign price = target.price | default: 1999
        assign available = target.available | default: false
        assign money_price = price | money
        if product_card_show_currency_code
            assign money_price = price | money_with_currency
        endif
    -%}

    <div class="sp-product-card-price
    {%- if available == false %} price-sold-out{% endif -%}
    {%- if compare_at_price > price and product.quantity_price_breaks_configured? != true %} price-on-sale{% endif -%}
    {%- if compare_at_price > price and product.quantity_price_breaks_configured? %} volume-pricing-sale-badge{% endif -%}
    {%- if product.price_varies == false and product.compare_at_price_varies %} price-no-compare{% endif -%}
  ">
        <span class="price-item price-item--sale price-item--last">
            {% if product_card_show_currency_code %}
                {{ price | money_with_currency }}
            {% else %}
                {{ price | money }}
            {% endif %}
        </span>
        {%- if show_compare_at_price and compare_at_price > 0 %}
            <span class="price-item price-item--regular">
            {% if product_card_show_currency_code %}
                {{ compare_at_price | money_with_currency }}
            {% else %}
                {{ compare_at_price | money }}
            {% endif %}
        </span>
        {%- endif -%}
    </div>
{% endif %}


                    {% if product_card_show_color_swatches %}
                        {% assign variants = product.variants %}
{% if product_card_show_color_swatches and variants %}
    {% assign colorSwatcheKey = shop.metaobjects['app--40489222145--shine_pages_settings']['shine_pages_settings'].colorSwatchKey.value | downcase %}
    {% assign colorSwatches = shop.metaobjects['app--40489222145--shine_pages_settings']['shine_pages_settings'].colorSwatches.value %}
    {% assign hasVariants = "" %}
    <div class="sp-product-color-swatches" data-alignment="{{ product_card_content_alignment }}"
         data-style="{{ product_card_color_swatch_style }}" data-has-change-image="1">
        {% assign setFirstVariant = false %}
        {% assign numItem = 0 %}
        {%- for option in product.options_with_values -%}
            {%- assign downcased_option = option.name | downcase -%}
            {%- if downcased_option == 'color' or downcased_option == 'colour' or downcased_option == 'couleur' or downcased_option == colorSwatcheKey -%}
                {%- assign variant_option = 'option' | append: forloop.index -%}

                {%- for value in option.values -%}
                    {%- assign downcased_value = value | downcase -%}
                    {%- capture color_id -%}{{ section.id }}-{{ product.id }}-{% increment color_index %}{%- endcapture -%}

                    {%- for variant in product.variants -%}
                        {%- if variant[variant_option] == value -%}
                            {% for color in colorSwatches %}
                                {% if value == color[0] %}
                                    {% assign color_check = color[0] | append: ',' %}
                                    {% unless hasVariants contains color_check %}
                                        {% assign numItem = numItem | plus: 1 %}
                                        {% assign hasVariants = hasVariants | append: color[0] | append: ',' %}
                                        {% if numItem <= max_color_swatches %}
                                            <div class="sp-product-color-swatch-item {% if setFirstVariant == false %}active{% endif %}"
                                                 data-plugin="sp-tooltip"
                                                 onclick="spProductCardColorSwatches(this)"
                                                    {% if variant.featured_image %}
                                                        data-featured-image="{{ variant.featured_image | image_url: width: 550 | default: '' }}"
                                                        data-featured-image-srcset="
                                                            {%- if variant.featured_image.width >= 165 -%}{{ variant.featured_image | image_url: width: 165 }} 165w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 360 -%}{{ variant.featured_image | image_url: width: 360 }} 360w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 533 -%}{{ variant.featured_image | image_url: width: 533 }} 533w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 720 -%}{{ variant.featured_image | image_url: width: 720 }} 720w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 940 -%}{{ variant.featured_image | image_url: width: 940 }} 940w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 1066 -%}{{ variant.featured_image | image_url: width: 1066 }} 1066w,{%- endif -%}
                                                            {{ variant.featured_image | image_url }} {{ variant.featured_image.width }}w"
                                                    {% endif %}
                                            >
                                                <div class="sp-product-color"
                                                     style="--sp-product-card-color-swatches-color: {{ color[1] }};">
                                                </div>
                                                {% if show_tooltip_color_swatch %}
                                                    <span class="sp-tooltip">{{ color[0] | strip }}</span>
                                                {% endif %}
                                            </div>
                                        {% endif %}
                                        {% assign setFirstVariant = true %}
                                    {% endunless %}
                                {% endif %}
                            {% endfor %}
                        {%- endif -%}
                    {%- endfor -%}
                {% endfor %}
            {% endif %}
        {%- endfor -%}
        {% if numItem > max_color_swatches %}
            <div class="sp-product-color-swatch-item color-none">
                <div class="sp-text">{{ numItem | minus: max_color_swatches | prepend: '+' }}</div>
            </div>
        {% endif %}
    </div>
{% endif %}
                    {% endif %}
                </div>
            </div>
        </div>
    {% endif %}
{% endif %}
                                                        </div>
                                                    {% endif %}
                                                    {% if setting.product_2 != null %}
                                                        {% assign product = setting.product_2 %}
                                                        <div class="sp-shop-the-look-product-item"
                                                             data-hotspot-id="sp-shop-the-look-product-item-2-{{ product.id }}-{{ block.id }}">
                                                            {% if product and product != empty %}
    {% capture productData %}
        {% assign onSale = false %}
        {% assign inventory = 0 %}
        {
        "id":{{- product.id -}},
        "available":{{- product.available | default: false -}},
        "title":{{- product.title | json -}},
        "handle":{{- product.handle | json -}},
        "url":"{{- product.url -}}",
        "type":{{- product.type | json -}},
        "price":{{- product.price | default: 0 -}},
        "price_max":{{- product.price_max | default: 0 -}},
        "price_min":{{- product.price_min | default: 0 -}},
        "compare_at_price":{{- product.compare_at_price | default: 0 -}},
        "compare_at_price_max": {{- product.compare_at_price_max | default: 0 -}},
        "compare_at_price_min": {{- product.compare_at_price_min | default: 0 -}},
        "price_varies": {{- product.price_varies | default: false -}},
        "compare_at_price_varies": {{- product.compare_at_price_varies | default: false -}},
        "created_at":"{{- product.created_at | date: "%s%L" -}}",
        "published_at":"{{- product.published_at | date: "%s%L" -}}",
        "tags": [{% for tag in product.tags %}{%- unless forloop.first -%},{%- endunless -%}{{- tag | json -}}{% endfor %}],
        "vendor": {{- product.vendor | json -}},
        "featured_image":"{{- product.featured_image -}}",
        "featured_media":"{% if product.featured_media %}{{- product.featured_media | image_url: width: 450 -}}{% endif %}",
        "images":[
        {%- for image in product.images -%}{%- unless forloop.first -%},
        {%- endunless -%}{"id":{{- image.id -}},
            "src":{{- image.src | json -}}}
        {%- endfor -%}],
        "collections":{{- product.collections | map: 'id' | json -}},
        {%- assign selectedVariant = product.selected_or_first_available_variant -%}
        {% assign sale_amount = 0 %}
        {% assign sale_percent = 0 %}
        {% if selectedVariant.price and selectedVariant.compare_at_price %}
            {% assign sale_amount = selectedVariant.compare_at_price | minus: selectedVariant.price %}
            {% if sale_amount > 0 %}
                {% assign divisor = selectedVariant.compare_at_price | times: 1.0 %}
                {% assign sale_percent = selectedVariant.compare_at_price | minus: selectedVariant.price | divided_by: divisor | times: 100.0 | round %}
            {% endif %}
        {% endif %}
        "variant": {
        "id": "{{ selectedVariant.id }}",
        "title": {{- selectedVariant.title | json -}},
        "available": {{- selectedVariant.available | default: false -}},
        "inventory_management": "{{- selectedVariant.inventory_management -}}",
        "inventory_policy": "{{- selectedVariant.inventory_policy -}}",
        "inventory_quantity": {{- selectedVariant.inventory_quantity | default: 0 -}},
        "price": {{- selectedVariant.price | default: 0 -}},
        "compare_at_price": {{- selectedVariant.compare_at_price | default: 0 -}},
        "unit_price": "{{- selectedVariant.unit_price -}}",
        "weight": "{{- selectedVariant.weight -}}",
        "weight_in_unit": "{{- selectedVariant.weight_in_unit -}}",
        "weight_unit": "{{- selectedVariant.weight_unit -}}",
        "sale_amount": {{- sale_amount | money | json -}},
        "sale_percent": {{- sale_percent | json -}}
        },
        "variants":{
        {%- for variant in product.variants -%}
            {%- unless forloop.first -%},{%- endunless -%}
            {% if variant.compare_at_price > variant.price %}
                {% assign onSale = true %}
            {% endif %}
            {% if variant.available and variant.inventory_management != null and variant.inventory_management != '' %}
                {% if variant.inventory_policy == 'deny' and variant.inventory_quantity > 0 %}
                    {% assign inventory = inventory | plus: variant.inventory_quantity %}
                {% elsif variant.inventory_policy == 'continue' %}
                    {% assign inventory = -1 %}
                {% endif %}
            {% elsif variant.available and variant.inventory_management == null %}
                {% assign inventory = -1 %}
            {% endif %}
            {% assign sale_amount = 0 %}
            {% assign sale_percent = 0 %}
            {% if variant.price and variant.compare_at_price %}
                {% assign sale_amount = variant.compare_at_price | minus: variant.price %}
                {% if sale_amount > 0 %}
                    {% assign divisor = variant.compare_at_price | times: 1.0 %}
                    {% assign sale_percent = variant.compare_at_price | minus: variant.price | divided_by: divisor | times: 100.0 | round %}
                {% endif %}
            {% endif %}
            "{{- variant.id -}}": {
            "id":{{- variant.id -}},
            "title":{{- variant.title | json }},
            "name":{{- variant.name | json }},
            "price":{{- variant.price | default: 0 -}},
            "compare_at_price":{{- variant.compare_at_price | default: 0 -}},
            "available":{{- variant.available | default: false -}},
            "sku":{{- variant.sku | json -}},
            "weight":"{{- variant.weight -}}",
            "weight_unit": "{{- variant.weight_unit -}}",
            "inventory_management": "{{- variant.inventory_management -}}",
            "inventory_policy": "{{- variant.inventory_policy -}}",
            "inventory_quantity": {{- variant.inventory_quantity | default: 0 -}},
            "selected": {%- if variant.selected -%}  {{- variant.selected -}} {%- elsif product.selected_or_first_available_variant.id == variant.id -%} true  {%- else -%} false {%- endif -%},
            "sale_amount": {{- sale_amount | money | json -}},
            "sale_percent": {{- sale_percent | json -}}
            }
        {%- endfor -%}
        },
        "on_sale":{{- onSale -}},
        "inventory":{{- inventory -}},
        "options":{{- product.options_with_values | json -}},
        "has_only_default_variant": {{ product.has_only_default_variant | default: false }}
        }
    {% endcapture %}
    <div class="sp-card sp-product-card"
         data-style="{{ product_card_style | default: "style-1" }}"
         data-content-alignment="{{ product_card_content_alignment }}"
         data-box-shadow="{{ product_card_box_shadow }}"
    >
        <div data-app="shine-trust-app" data-product="{{- productData | base64_encode -}}"></div>
        <div class="sp-card-inner">
            <div class="sp-card-media sp-product-card-media {% if product.media[1] != null and product_card_show_second_image %}has-second-image{% endif %}">
                <div class="sp-product-card-badge" data-alignment="vertical">
                    {% if product_card_show_discount_badge and product.compare_at_price > product.price %}
                        {% assign save = product.compare_at_price | minus: product.price | money %}
                        {% if product_card_show_currency_code %}
                            {% assign save = product.compare_at_price | minus: product.price | money_with_currency %}
                        {% endif %}
                        {% if product_card_discount_badge_type == 'percent' %}
                            {% assign save = product.compare_at_price | minus: product.price | times: 1.0 | divided_by: product.compare_at_price | times: 100 | round: 0 | append: "%" %}
                        {% endif %}
                        <span class="sp-badge sp-badge--on-sale">{{ 'shinepages.product.sale_html' | t: price: save }}</span>
                    {% endif %}
                    {% if product_card_show_sold_out_badge and product.available == false %}
                        <span class="sp-badge sp-badge--sold-out">{{ 'shinepages.product.sold_out' | t }}</span>
                    {% endif %}
                </div>
                <a class="sp-link" href="{{ product.url }}">
                    <img srcset="
                  {%- if product.featured_media.width >= 165 -%}{{ product.featured_media | image_url: width: 165 }} 165w,{%- endif -%}
                  {%- if product.featured_media.width >= 360 -%}{{ product.featured_media | image_url: width: 360 }} 360w,{%- endif -%}
                  {%- if product.featured_media.width >= 533 -%}{{ product.featured_media | image_url: width: 533 }} 533w,{%- endif -%}
                  {%- if product.featured_media.width >= 720 -%}{{ product.featured_media | image_url: width: 720 }} 720w,{%- endif -%}
                  {%- if product.featured_media.width >= 940 -%}{{ product.featured_media | image_url: width: 940 }} 940w,{%- endif -%}
                  {%- if product.featured_media.width >= 1066 -%}{{ product.featured_media | image_url: width: 1066 }} 1066w,{%- endif -%}
                  {{ product.featured_media | image_url }} {{ product.featured_media.width }}w
                "
                         src="{{ product.featured_media | image_url: width: 550 }}"
                         sizes="(min-width: 1200px) 1200px, (min-width: 990px) 990px, (min-width: 750px) 750px, 650px)"
                         alt="{{ product.featured_media.alt | escape }}"
                         width="{{ product.featured_media.width }}"
                         height="{{ product.featured_media.height }}"
                         data-change-variant-image="{{ product.featured_media | image_url: width: 550 }}"
                         data-change-variant-image-set="
                  {%- if product.featured_media.width >= 165 -%}{{ product.featured_media | image_url: width: 165 }} 165w,{%- endif -%}
                  {%- if product.featured_media.width >= 360 -%}{{ product.featured_media | image_url: width: 360 }} 360w,{%- endif -%}
                  {%- if product.featured_media.width >= 533 -%}{{ product.featured_media | image_url: width: 533 }} 533w,{%- endif -%}
                  {%- if product.featured_media.width >= 720 -%}{{ product.featured_media | image_url: width: 720 }} 720w,{%- endif -%}
                  {%- if product.featured_media.width >= 940 -%}{{ product.featured_media | image_url: width: 940 }} 940w,{%- endif -%}
                  {%- if product.featured_media.width >= 1066 -%}{{ product.featured_media | image_url: width: 1066 }} 1066w,{%- endif -%}
                  {{ product.featured_media | image_url }} {{ product.featured_media.width }}w
                "
                    >
                    {%- if product.media[1] != null and product_card_show_second_image -%}
                        <img
                                srcset="
                    {%- if product.media[1].width >= 165 -%}{{ product.media[1] | image_url: width: 165 }} 165w,{%- endif -%}
                    {%- if product.media[1].width >= 360 -%}{{ product.media[1] | image_url: width: 360 }} 360w,{%- endif -%}
                    {%- if product.media[1].width >= 533 -%}{{ product.media[1] | image_url: width: 533 }} 533w,{%- endif -%}
                    {%- if product.media[1].width >= 720 -%}{{ product.media[1] | image_url: width: 720 }} 720w,{%- endif -%}
                    {%- if product.media[1].width >= 940 -%}{{ product.media[1] | image_url: width: 940 }} 940w,{%- endif -%}
                    {%- if product.media[1].width >= 1066 -%}{{ product.media[1] | image_url: width: 1066 }} 1066w,{%- endif -%}
                    {{ product.media[1] | image_url }} {{ product.media[1].width }}w
                  "
                                src="{{ product.media[1] | image_url: width: 1080 }}"
                                sizes="(min-width: {{ settings.page_width }}px) {{ settings.page_width | minus: 130 | divided_by: 4 }}px, (min-width: 990px) calc((100vw - 130px) / 4), (min-width: 750px) calc((100vw - 120px) / 3), calc((100vw - 35px) / 2)"
                                alt=""
                                width="{{ product.media[1].width }}"
                                height="{{ product.media[1].height }}"
                        >
                    {%- endif -%}
                </a>
            </div>
            <div class="sp-card-information sp-product-card-information sp-text-alignment-{{ text_alignment }}">
                {% if product_card_show_vendor %}
                    <div class="sp-product-card-vendor">{{ product.vendor }}</div>
                {% endif %}
                <h3 class="sp-product-card-heading">
                    <a class="sp-link" href="{{ product.url }}">{{ product.title | escape }}</a>
                </h3>
                {% if product_card_show_rating and product.metafields.reviews.rating.value != blank %}
    <div class="sp-product-card-rating">
        {% liquid
            assign rating_decimal = 0
            assign decimal = product.metafields.reviews.rating.value.rating | modulo: 1
            if decimal >= 0.3 and decimal <= 0.7
                assign rating_decimal = 0.5
            elsif decimal > 0.7
                assign rating_decimal = 1
            endif
        %}
        <div class="sp-rating" role="img">
            <span aria-hidden="true" class="sp-rating-star"
                  style="--sp-rating: {{ product.metafields.reviews.rating.value.rating | floor }}; --sp-rating-max: {{ product.metafields.reviews.rating.value.scale_max }}; --sp-rating-decimal: {{ rating_decimal }};"></span>
            {% if product_card_show_rating_number %}
                <span class="sp-rating-number">({{ product.metafields.reviews.rating.value.rating | floor }})</span>
                {% endif %}
        </div>
    </div>
{%- endif -%}
                {% comment %}
  Renders a list of product's price (regular, sale)

  Accepts:
  - product: {Object} Product Liquid object (optional)
  - use_variant: {Boolean} Renders selected or first variant price instead of overall product pricing (optional)
  - price_class: {String} Adds a price class to the price element (optional)
  - show_compare_at_price: {Boolean} Renders the compare at price if the product matches the condition (optional)
  Usage:
  {% render 'price', product: product %}
{% endcomment %}
{% if product_card_show_price %}
    {%- liquid
        if use_first_variant_product
            assign target = product.selected_or_first_available_variant
        else
            assign target = product
        endif

        assign compare_at_price = target.compare_at_price
        assign price = target.price | default: 1999
        assign available = target.available | default: false
        assign money_price = price | money
        if product_card_show_currency_code
            assign money_price = price | money_with_currency
        endif
    -%}

    <div class="sp-product-card-price
    {%- if available == false %} price-sold-out{% endif -%}
    {%- if compare_at_price > price and product.quantity_price_breaks_configured? != true %} price-on-sale{% endif -%}
    {%- if compare_at_price > price and product.quantity_price_breaks_configured? %} volume-pricing-sale-badge{% endif -%}
    {%- if product.price_varies == false and product.compare_at_price_varies %} price-no-compare{% endif -%}
  ">
        <span class="price-item price-item--sale price-item--last">
            {% if product_card_show_currency_code %}
                {{ price | money_with_currency }}
            {% else %}
                {{ price | money }}
            {% endif %}
        </span>
        {%- if show_compare_at_price and compare_at_price > 0 %}
            <span class="price-item price-item--regular">
            {% if product_card_show_currency_code %}
                {{ compare_at_price | money_with_currency }}
            {% else %}
                {{ compare_at_price | money }}
            {% endif %}
        </span>
        {%- endif -%}
    </div>
{% endif %}

                {% assign variants = product.variants %}
{% if product_card_show_color_swatches and variants %}
    {% assign colorSwatcheKey = shop.metaobjects['app--40489222145--shine_pages_settings']['shine_pages_settings'].colorSwatchKey.value | downcase %}
    {% assign colorSwatches = shop.metaobjects['app--40489222145--shine_pages_settings']['shine_pages_settings'].colorSwatches.value %}
    {% assign hasVariants = "" %}
    <div class="sp-product-color-swatches" data-alignment="{{ product_card_content_alignment }}"
         data-style="{{ product_card_color_swatch_style }}" data-has-change-image="1">
        {% assign setFirstVariant = false %}
        {% assign numItem = 0 %}
        {%- for option in product.options_with_values -%}
            {%- assign downcased_option = option.name | downcase -%}
            {%- if downcased_option == 'color' or downcased_option == 'colour' or downcased_option == 'couleur' or downcased_option == colorSwatcheKey -%}
                {%- assign variant_option = 'option' | append: forloop.index -%}

                {%- for value in option.values -%}
                    {%- assign downcased_value = value | downcase -%}
                    {%- capture color_id -%}{{ section.id }}-{{ product.id }}-{% increment color_index %}{%- endcapture -%}

                    {%- for variant in product.variants -%}
                        {%- if variant[variant_option] == value -%}
                            {% for color in colorSwatches %}
                                {% if value == color[0] %}
                                    {% assign color_check = color[0] | append: ',' %}
                                    {% unless hasVariants contains color_check %}
                                        {% assign numItem = numItem | plus: 1 %}
                                        {% assign hasVariants = hasVariants | append: color[0] | append: ',' %}
                                        {% if numItem <= max_color_swatches %}
                                            <div class="sp-product-color-swatch-item {% if setFirstVariant == false %}active{% endif %}"
                                                 data-plugin="sp-tooltip"
                                                 onclick="spProductCardColorSwatches(this)"
                                                    {% if variant.featured_image %}
                                                        data-featured-image="{{ variant.featured_image | image_url: width: 550 | default: '' }}"
                                                        data-featured-image-srcset="
                                                            {%- if variant.featured_image.width >= 165 -%}{{ variant.featured_image | image_url: width: 165 }} 165w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 360 -%}{{ variant.featured_image | image_url: width: 360 }} 360w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 533 -%}{{ variant.featured_image | image_url: width: 533 }} 533w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 720 -%}{{ variant.featured_image | image_url: width: 720 }} 720w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 940 -%}{{ variant.featured_image | image_url: width: 940 }} 940w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 1066 -%}{{ variant.featured_image | image_url: width: 1066 }} 1066w,{%- endif -%}
                                                            {{ variant.featured_image | image_url }} {{ variant.featured_image.width }}w"
                                                    {% endif %}
                                            >
                                                <div class="sp-product-color"
                                                     style="--sp-product-card-color-swatches-color: {{ color[1] }};">
                                                </div>
                                                {% if show_tooltip_color_swatch %}
                                                    <span class="sp-tooltip">{{ color[0] | strip }}</span>
                                                {% endif %}
                                            </div>
                                        {% endif %}
                                        {% assign setFirstVariant = true %}
                                    {% endunless %}
                                {% endif %}
                            {% endfor %}
                        {%- endif -%}
                    {%- endfor -%}
                {% endfor %}
            {% endif %}
        {%- endfor -%}
        {% if numItem > max_color_swatches %}
            <div class="sp-product-color-swatch-item color-none">
                <div class="sp-text">{{ numItem | minus: max_color_swatches | prepend: '+' }}</div>
            </div>
        {% endif %}
    </div>
{% endif %}
            </div>
        </div>
    </div>
{% else %}
    {% if placeholder_product %}
        <div class="sp-card sp-product-card"
             data-style="{{ product_card_style | default: "style-1" }}"
             data-content-alignment="{{ product_card_content_alignment }}"
             data-box-shadow="{{ product_card_box_shadow }}"
        >
            <div class="sp-card-inner">
                <div class="sp-card-media sp-product-card-media {% if product_card_show_second_image %}has-second-image{% endif %}">
                    <div class="sp-product-card-badge" data-alignment="vertical">
                        {% if product_card_show_sold_out_badge %}
                            <span class="sp-badge sp-badge--sold-out">{{ 'shinepages.product.sold_out' | t }}</span>
                        {% endif %}
                        {% if product_card_show_discount_badge %}
                            {% assign save = 100.00 | money %}
                            {% if currency_code_enabled %}
                                {% assign save = 100.00 | money_with_currency %}
                            {% endif %}
                            {% if product_card_discount_badge_type == 'percent' %}
                                {% assign save = "10%" %}
                            {% endif %}
                            <span class="sp-badge sp-badge--on-sale">{{ 'shinepages.product.sale_html' | t: price: save }}</span>
                        {% endif %}
                    </div>
                    <a href="#.">
                        {{ 'product-1' | placeholder_svg_tag: 'sp-placeholder-product-1-light' }}
                        {% if product_card_show_second_image %}
                            {{ 'product-2' | placeholder_svg_tag: 'sp-placeholder-product-1-light' }}
                        {% endif %}
                    </a>
                </div>
                <div class="sp-card-information sp-product-card-information">
                    {% if product_card_show_vendor %}
                        <div class="sp-product-card-vendor">shinepages</div>
                    {% endif %}
                    <h3 class="sp-product-card-heading">
                        <a href="#.">Example Product Title</a>
                    </h3>
                    {% if product_card_show_rating %}
                        {% if product_card_show_rating and product.metafields.reviews.rating.value != blank %}
    <div class="sp-product-card-rating">
        {% liquid
            assign rating_decimal = 0
            assign decimal = product.metafields.reviews.rating.value.rating | modulo: 1
            if decimal >= 0.3 and decimal <= 0.7
                assign rating_decimal = 0.5
            elsif decimal > 0.7
                assign rating_decimal = 1
            endif
        %}
        <div class="sp-rating" role="img">
            <span aria-hidden="true" class="sp-rating-star"
                  style="--sp-rating: {{ product.metafields.reviews.rating.value.rating | floor }}; --sp-rating-max: {{ product.metafields.reviews.rating.value.scale_max }}; --sp-rating-decimal: {{ rating_decimal }};"></span>
            {% if product_card_show_rating_number %}
                <span class="sp-rating-number">({{ product.metafields.reviews.rating.value.rating | floor }})</span>
                {% endif %}
        </div>
    </div>
{%- endif -%}
                    {% endif %}

                    {% assign use_variant = true %}
                    {% assign show_compare_at_price = true %}
                    {% assign currency_code_enabled = true %}
                    {% comment %}
  Renders a list of product's price (regular, sale)

  Accepts:
  - product: {Object} Product Liquid object (optional)
  - use_variant: {Boolean} Renders selected or first variant price instead of overall product pricing (optional)
  - price_class: {String} Adds a price class to the price element (optional)
  - show_compare_at_price: {Boolean} Renders the compare at price if the product matches the condition (optional)
  Usage:
  {% render 'price', product: product %}
{% endcomment %}
{% if product_card_show_price %}
    {%- liquid
        if use_first_variant_product
            assign target = product.selected_or_first_available_variant
        else
            assign target = product
        endif

        assign compare_at_price = target.compare_at_price
        assign price = target.price | default: 1999
        assign available = target.available | default: false
        assign money_price = price | money
        if product_card_show_currency_code
            assign money_price = price | money_with_currency
        endif
    -%}

    <div class="sp-product-card-price
    {%- if available == false %} price-sold-out{% endif -%}
    {%- if compare_at_price > price and product.quantity_price_breaks_configured? != true %} price-on-sale{% endif -%}
    {%- if compare_at_price > price and product.quantity_price_breaks_configured? %} volume-pricing-sale-badge{% endif -%}
    {%- if product.price_varies == false and product.compare_at_price_varies %} price-no-compare{% endif -%}
  ">
        <span class="price-item price-item--sale price-item--last">
            {% if product_card_show_currency_code %}
                {{ price | money_with_currency }}
            {% else %}
                {{ price | money }}
            {% endif %}
        </span>
        {%- if show_compare_at_price and compare_at_price > 0 %}
            <span class="price-item price-item--regular">
            {% if product_card_show_currency_code %}
                {{ compare_at_price | money_with_currency }}
            {% else %}
                {{ compare_at_price | money }}
            {% endif %}
        </span>
        {%- endif -%}
    </div>
{% endif %}


                    {% if product_card_show_color_swatches %}
                        {% assign variants = product.variants %}
{% if product_card_show_color_swatches and variants %}
    {% assign colorSwatcheKey = shop.metaobjects['app--40489222145--shine_pages_settings']['shine_pages_settings'].colorSwatchKey.value | downcase %}
    {% assign colorSwatches = shop.metaobjects['app--40489222145--shine_pages_settings']['shine_pages_settings'].colorSwatches.value %}
    {% assign hasVariants = "" %}
    <div class="sp-product-color-swatches" data-alignment="{{ product_card_content_alignment }}"
         data-style="{{ product_card_color_swatch_style }}" data-has-change-image="1">
        {% assign setFirstVariant = false %}
        {% assign numItem = 0 %}
        {%- for option in product.options_with_values -%}
            {%- assign downcased_option = option.name | downcase -%}
            {%- if downcased_option == 'color' or downcased_option == 'colour' or downcased_option == 'couleur' or downcased_option == colorSwatcheKey -%}
                {%- assign variant_option = 'option' | append: forloop.index -%}

                {%- for value in option.values -%}
                    {%- assign downcased_value = value | downcase -%}
                    {%- capture color_id -%}{{ section.id }}-{{ product.id }}-{% increment color_index %}{%- endcapture -%}

                    {%- for variant in product.variants -%}
                        {%- if variant[variant_option] == value -%}
                            {% for color in colorSwatches %}
                                {% if value == color[0] %}
                                    {% assign color_check = color[0] | append: ',' %}
                                    {% unless hasVariants contains color_check %}
                                        {% assign numItem = numItem | plus: 1 %}
                                        {% assign hasVariants = hasVariants | append: color[0] | append: ',' %}
                                        {% if numItem <= max_color_swatches %}
                                            <div class="sp-product-color-swatch-item {% if setFirstVariant == false %}active{% endif %}"
                                                 data-plugin="sp-tooltip"
                                                 onclick="spProductCardColorSwatches(this)"
                                                    {% if variant.featured_image %}
                                                        data-featured-image="{{ variant.featured_image | image_url: width: 550 | default: '' }}"
                                                        data-featured-image-srcset="
                                                            {%- if variant.featured_image.width >= 165 -%}{{ variant.featured_image | image_url: width: 165 }} 165w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 360 -%}{{ variant.featured_image | image_url: width: 360 }} 360w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 533 -%}{{ variant.featured_image | image_url: width: 533 }} 533w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 720 -%}{{ variant.featured_image | image_url: width: 720 }} 720w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 940 -%}{{ variant.featured_image | image_url: width: 940 }} 940w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 1066 -%}{{ variant.featured_image | image_url: width: 1066 }} 1066w,{%- endif -%}
                                                            {{ variant.featured_image | image_url }} {{ variant.featured_image.width }}w"
                                                    {% endif %}
                                            >
                                                <div class="sp-product-color"
                                                     style="--sp-product-card-color-swatches-color: {{ color[1] }};">
                                                </div>
                                                {% if show_tooltip_color_swatch %}
                                                    <span class="sp-tooltip">{{ color[0] | strip }}</span>
                                                {% endif %}
                                            </div>
                                        {% endif %}
                                        {% assign setFirstVariant = true %}
                                    {% endunless %}
                                {% endif %}
                            {% endfor %}
                        {%- endif -%}
                    {%- endfor -%}
                {% endfor %}
            {% endif %}
        {%- endfor -%}
        {% if numItem > max_color_swatches %}
            <div class="sp-product-color-swatch-item color-none">
                <div class="sp-text">{{ numItem | minus: max_color_swatches | prepend: '+' }}</div>
            </div>
        {% endif %}
    </div>
{% endif %}
                    {% endif %}
                </div>
            </div>
        </div>
    {% endif %}
{% endif %}
                                                        </div>
                                                    {% endif %}
                                                    {% if setting.product_3 != null %}
                                                        {% assign product = setting.product_3 %}
                                                        <div class="sp-shop-the-look-product-item"
                                                             data-hotspot-id="sp-shop-the-look-product-item-3-{{ product.id }}-{{ block.id }}">
                                                            {% if product and product != empty %}
    {% capture productData %}
        {% assign onSale = false %}
        {% assign inventory = 0 %}
        {
        "id":{{- product.id -}},
        "available":{{- product.available | default: false -}},
        "title":{{- product.title | json -}},
        "handle":{{- product.handle | json -}},
        "url":"{{- product.url -}}",
        "type":{{- product.type | json -}},
        "price":{{- product.price | default: 0 -}},
        "price_max":{{- product.price_max | default: 0 -}},
        "price_min":{{- product.price_min | default: 0 -}},
        "compare_at_price":{{- product.compare_at_price | default: 0 -}},
        "compare_at_price_max": {{- product.compare_at_price_max | default: 0 -}},
        "compare_at_price_min": {{- product.compare_at_price_min | default: 0 -}},
        "price_varies": {{- product.price_varies | default: false -}},
        "compare_at_price_varies": {{- product.compare_at_price_varies | default: false -}},
        "created_at":"{{- product.created_at | date: "%s%L" -}}",
        "published_at":"{{- product.published_at | date: "%s%L" -}}",
        "tags": [{% for tag in product.tags %}{%- unless forloop.first -%},{%- endunless -%}{{- tag | json -}}{% endfor %}],
        "vendor": {{- product.vendor | json -}},
        "featured_image":"{{- product.featured_image -}}",
        "featured_media":"{% if product.featured_media %}{{- product.featured_media | image_url: width: 450 -}}{% endif %}",
        "images":[
        {%- for image in product.images -%}{%- unless forloop.first -%},
        {%- endunless -%}{"id":{{- image.id -}},
            "src":{{- image.src | json -}}}
        {%- endfor -%}],
        "collections":{{- product.collections | map: 'id' | json -}},
        {%- assign selectedVariant = product.selected_or_first_available_variant -%}
        {% assign sale_amount = 0 %}
        {% assign sale_percent = 0 %}
        {% if selectedVariant.price and selectedVariant.compare_at_price %}
            {% assign sale_amount = selectedVariant.compare_at_price | minus: selectedVariant.price %}
            {% if sale_amount > 0 %}
                {% assign divisor = selectedVariant.compare_at_price | times: 1.0 %}
                {% assign sale_percent = selectedVariant.compare_at_price | minus: selectedVariant.price | divided_by: divisor | times: 100.0 | round %}
            {% endif %}
        {% endif %}
        "variant": {
        "id": "{{ selectedVariant.id }}",
        "title": {{- selectedVariant.title | json -}},
        "available": {{- selectedVariant.available | default: false -}},
        "inventory_management": "{{- selectedVariant.inventory_management -}}",
        "inventory_policy": "{{- selectedVariant.inventory_policy -}}",
        "inventory_quantity": {{- selectedVariant.inventory_quantity | default: 0 -}},
        "price": {{- selectedVariant.price | default: 0 -}},
        "compare_at_price": {{- selectedVariant.compare_at_price | default: 0 -}},
        "unit_price": "{{- selectedVariant.unit_price -}}",
        "weight": "{{- selectedVariant.weight -}}",
        "weight_in_unit": "{{- selectedVariant.weight_in_unit -}}",
        "weight_unit": "{{- selectedVariant.weight_unit -}}",
        "sale_amount": {{- sale_amount | money | json -}},
        "sale_percent": {{- sale_percent | json -}}
        },
        "variants":{
        {%- for variant in product.variants -%}
            {%- unless forloop.first -%},{%- endunless -%}
            {% if variant.compare_at_price > variant.price %}
                {% assign onSale = true %}
            {% endif %}
            {% if variant.available and variant.inventory_management != null and variant.inventory_management != '' %}
                {% if variant.inventory_policy == 'deny' and variant.inventory_quantity > 0 %}
                    {% assign inventory = inventory | plus: variant.inventory_quantity %}
                {% elsif variant.inventory_policy == 'continue' %}
                    {% assign inventory = -1 %}
                {% endif %}
            {% elsif variant.available and variant.inventory_management == null %}
                {% assign inventory = -1 %}
            {% endif %}
            {% assign sale_amount = 0 %}
            {% assign sale_percent = 0 %}
            {% if variant.price and variant.compare_at_price %}
                {% assign sale_amount = variant.compare_at_price | minus: variant.price %}
                {% if sale_amount > 0 %}
                    {% assign divisor = variant.compare_at_price | times: 1.0 %}
                    {% assign sale_percent = variant.compare_at_price | minus: variant.price | divided_by: divisor | times: 100.0 | round %}
                {% endif %}
            {% endif %}
            "{{- variant.id -}}": {
            "id":{{- variant.id -}},
            "title":{{- variant.title | json }},
            "name":{{- variant.name | json }},
            "price":{{- variant.price | default: 0 -}},
            "compare_at_price":{{- variant.compare_at_price | default: 0 -}},
            "available":{{- variant.available | default: false -}},
            "sku":{{- variant.sku | json -}},
            "weight":"{{- variant.weight -}}",
            "weight_unit": "{{- variant.weight_unit -}}",
            "inventory_management": "{{- variant.inventory_management -}}",
            "inventory_policy": "{{- variant.inventory_policy -}}",
            "inventory_quantity": {{- variant.inventory_quantity | default: 0 -}},
            "selected": {%- if variant.selected -%}  {{- variant.selected -}} {%- elsif product.selected_or_first_available_variant.id == variant.id -%} true  {%- else -%} false {%- endif -%},
            "sale_amount": {{- sale_amount | money | json -}},
            "sale_percent": {{- sale_percent | json -}}
            }
        {%- endfor -%}
        },
        "on_sale":{{- onSale -}},
        "inventory":{{- inventory -}},
        "options":{{- product.options_with_values | json -}},
        "has_only_default_variant": {{ product.has_only_default_variant | default: false }}
        }
    {% endcapture %}
    <div class="sp-card sp-product-card"
         data-style="{{ product_card_style | default: "style-1" }}"
         data-content-alignment="{{ product_card_content_alignment }}"
         data-box-shadow="{{ product_card_box_shadow }}"
    >
        <div data-app="shine-trust-app" data-product="{{- productData | base64_encode -}}"></div>
        <div class="sp-card-inner">
            <div class="sp-card-media sp-product-card-media {% if product.media[1] != null and product_card_show_second_image %}has-second-image{% endif %}">
                <div class="sp-product-card-badge" data-alignment="vertical">
                    {% if product_card_show_discount_badge and product.compare_at_price > product.price %}
                        {% assign save = product.compare_at_price | minus: product.price | money %}
                        {% if product_card_show_currency_code %}
                            {% assign save = product.compare_at_price | minus: product.price | money_with_currency %}
                        {% endif %}
                        {% if product_card_discount_badge_type == 'percent' %}
                            {% assign save = product.compare_at_price | minus: product.price | times: 1.0 | divided_by: product.compare_at_price | times: 100 | round: 0 | append: "%" %}
                        {% endif %}
                        <span class="sp-badge sp-badge--on-sale">{{ 'shinepages.product.sale_html' | t: price: save }}</span>
                    {% endif %}
                    {% if product_card_show_sold_out_badge and product.available == false %}
                        <span class="sp-badge sp-badge--sold-out">{{ 'shinepages.product.sold_out' | t }}</span>
                    {% endif %}
                </div>
                <a class="sp-link" href="{{ product.url }}">
                    <img srcset="
                  {%- if product.featured_media.width >= 165 -%}{{ product.featured_media | image_url: width: 165 }} 165w,{%- endif -%}
                  {%- if product.featured_media.width >= 360 -%}{{ product.featured_media | image_url: width: 360 }} 360w,{%- endif -%}
                  {%- if product.featured_media.width >= 533 -%}{{ product.featured_media | image_url: width: 533 }} 533w,{%- endif -%}
                  {%- if product.featured_media.width >= 720 -%}{{ product.featured_media | image_url: width: 720 }} 720w,{%- endif -%}
                  {%- if product.featured_media.width >= 940 -%}{{ product.featured_media | image_url: width: 940 }} 940w,{%- endif -%}
                  {%- if product.featured_media.width >= 1066 -%}{{ product.featured_media | image_url: width: 1066 }} 1066w,{%- endif -%}
                  {{ product.featured_media | image_url }} {{ product.featured_media.width }}w
                "
                         src="{{ product.featured_media | image_url: width: 550 }}"
                         sizes="(min-width: 1200px) 1200px, (min-width: 990px) 990px, (min-width: 750px) 750px, 650px)"
                         alt="{{ product.featured_media.alt | escape }}"
                         width="{{ product.featured_media.width }}"
                         height="{{ product.featured_media.height }}"
                         data-change-variant-image="{{ product.featured_media | image_url: width: 550 }}"
                         data-change-variant-image-set="
                  {%- if product.featured_media.width >= 165 -%}{{ product.featured_media | image_url: width: 165 }} 165w,{%- endif -%}
                  {%- if product.featured_media.width >= 360 -%}{{ product.featured_media | image_url: width: 360 }} 360w,{%- endif -%}
                  {%- if product.featured_media.width >= 533 -%}{{ product.featured_media | image_url: width: 533 }} 533w,{%- endif -%}
                  {%- if product.featured_media.width >= 720 -%}{{ product.featured_media | image_url: width: 720 }} 720w,{%- endif -%}
                  {%- if product.featured_media.width >= 940 -%}{{ product.featured_media | image_url: width: 940 }} 940w,{%- endif -%}
                  {%- if product.featured_media.width >= 1066 -%}{{ product.featured_media | image_url: width: 1066 }} 1066w,{%- endif -%}
                  {{ product.featured_media | image_url }} {{ product.featured_media.width }}w
                "
                    >
                    {%- if product.media[1] != null and product_card_show_second_image -%}
                        <img
                                srcset="
                    {%- if product.media[1].width >= 165 -%}{{ product.media[1] | image_url: width: 165 }} 165w,{%- endif -%}
                    {%- if product.media[1].width >= 360 -%}{{ product.media[1] | image_url: width: 360 }} 360w,{%- endif -%}
                    {%- if product.media[1].width >= 533 -%}{{ product.media[1] | image_url: width: 533 }} 533w,{%- endif -%}
                    {%- if product.media[1].width >= 720 -%}{{ product.media[1] | image_url: width: 720 }} 720w,{%- endif -%}
                    {%- if product.media[1].width >= 940 -%}{{ product.media[1] | image_url: width: 940 }} 940w,{%- endif -%}
                    {%- if product.media[1].width >= 1066 -%}{{ product.media[1] | image_url: width: 1066 }} 1066w,{%- endif -%}
                    {{ product.media[1] | image_url }} {{ product.media[1].width }}w
                  "
                                src="{{ product.media[1] | image_url: width: 1080 }}"
                                sizes="(min-width: {{ settings.page_width }}px) {{ settings.page_width | minus: 130 | divided_by: 4 }}px, (min-width: 990px) calc((100vw - 130px) / 4), (min-width: 750px) calc((100vw - 120px) / 3), calc((100vw - 35px) / 2)"
                                alt=""
                                width="{{ product.media[1].width }}"
                                height="{{ product.media[1].height }}"
                        >
                    {%- endif -%}
                </a>
            </div>
            <div class="sp-card-information sp-product-card-information sp-text-alignment-{{ text_alignment }}">
                {% if product_card_show_vendor %}
                    <div class="sp-product-card-vendor">{{ product.vendor }}</div>
                {% endif %}
                <h3 class="sp-product-card-heading">
                    <a class="sp-link" href="{{ product.url }}">{{ product.title | escape }}</a>
                </h3>
                {% if product_card_show_rating and product.metafields.reviews.rating.value != blank %}
    <div class="sp-product-card-rating">
        {% liquid
            assign rating_decimal = 0
            assign decimal = product.metafields.reviews.rating.value.rating | modulo: 1
            if decimal >= 0.3 and decimal <= 0.7
                assign rating_decimal = 0.5
            elsif decimal > 0.7
                assign rating_decimal = 1
            endif
        %}
        <div class="sp-rating" role="img">
            <span aria-hidden="true" class="sp-rating-star"
                  style="--sp-rating: {{ product.metafields.reviews.rating.value.rating | floor }}; --sp-rating-max: {{ product.metafields.reviews.rating.value.scale_max }}; --sp-rating-decimal: {{ rating_decimal }};"></span>
            {% if product_card_show_rating_number %}
                <span class="sp-rating-number">({{ product.metafields.reviews.rating.value.rating | floor }})</span>
                {% endif %}
        </div>
    </div>
{%- endif -%}
                {% comment %}
  Renders a list of product's price (regular, sale)

  Accepts:
  - product: {Object} Product Liquid object (optional)
  - use_variant: {Boolean} Renders selected or first variant price instead of overall product pricing (optional)
  - price_class: {String} Adds a price class to the price element (optional)
  - show_compare_at_price: {Boolean} Renders the compare at price if the product matches the condition (optional)
  Usage:
  {% render 'price', product: product %}
{% endcomment %}
{% if product_card_show_price %}
    {%- liquid
        if use_first_variant_product
            assign target = product.selected_or_first_available_variant
        else
            assign target = product
        endif

        assign compare_at_price = target.compare_at_price
        assign price = target.price | default: 1999
        assign available = target.available | default: false
        assign money_price = price | money
        if product_card_show_currency_code
            assign money_price = price | money_with_currency
        endif
    -%}

    <div class="sp-product-card-price
    {%- if available == false %} price-sold-out{% endif -%}
    {%- if compare_at_price > price and product.quantity_price_breaks_configured? != true %} price-on-sale{% endif -%}
    {%- if compare_at_price > price and product.quantity_price_breaks_configured? %} volume-pricing-sale-badge{% endif -%}
    {%- if product.price_varies == false and product.compare_at_price_varies %} price-no-compare{% endif -%}
  ">
        <span class="price-item price-item--sale price-item--last">
            {% if product_card_show_currency_code %}
                {{ price | money_with_currency }}
            {% else %}
                {{ price | money }}
            {% endif %}
        </span>
        {%- if show_compare_at_price and compare_at_price > 0 %}
            <span class="price-item price-item--regular">
            {% if product_card_show_currency_code %}
                {{ compare_at_price | money_with_currency }}
            {% else %}
                {{ compare_at_price | money }}
            {% endif %}
        </span>
        {%- endif -%}
    </div>
{% endif %}

                {% assign variants = product.variants %}
{% if product_card_show_color_swatches and variants %}
    {% assign colorSwatcheKey = shop.metaobjects['app--40489222145--shine_pages_settings']['shine_pages_settings'].colorSwatchKey.value | downcase %}
    {% assign colorSwatches = shop.metaobjects['app--40489222145--shine_pages_settings']['shine_pages_settings'].colorSwatches.value %}
    {% assign hasVariants = "" %}
    <div class="sp-product-color-swatches" data-alignment="{{ product_card_content_alignment }}"
         data-style="{{ product_card_color_swatch_style }}" data-has-change-image="1">
        {% assign setFirstVariant = false %}
        {% assign numItem = 0 %}
        {%- for option in product.options_with_values -%}
            {%- assign downcased_option = option.name | downcase -%}
            {%- if downcased_option == 'color' or downcased_option == 'colour' or downcased_option == 'couleur' or downcased_option == colorSwatcheKey -%}
                {%- assign variant_option = 'option' | append: forloop.index -%}

                {%- for value in option.values -%}
                    {%- assign downcased_value = value | downcase -%}
                    {%- capture color_id -%}{{ section.id }}-{{ product.id }}-{% increment color_index %}{%- endcapture -%}

                    {%- for variant in product.variants -%}
                        {%- if variant[variant_option] == value -%}
                            {% for color in colorSwatches %}
                                {% if value == color[0] %}
                                    {% assign color_check = color[0] | append: ',' %}
                                    {% unless hasVariants contains color_check %}
                                        {% assign numItem = numItem | plus: 1 %}
                                        {% assign hasVariants = hasVariants | append: color[0] | append: ',' %}
                                        {% if numItem <= max_color_swatches %}
                                            <div class="sp-product-color-swatch-item {% if setFirstVariant == false %}active{% endif %}"
                                                 data-plugin="sp-tooltip"
                                                 onclick="spProductCardColorSwatches(this)"
                                                    {% if variant.featured_image %}
                                                        data-featured-image="{{ variant.featured_image | image_url: width: 550 | default: '' }}"
                                                        data-featured-image-srcset="
                                                            {%- if variant.featured_image.width >= 165 -%}{{ variant.featured_image | image_url: width: 165 }} 165w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 360 -%}{{ variant.featured_image | image_url: width: 360 }} 360w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 533 -%}{{ variant.featured_image | image_url: width: 533 }} 533w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 720 -%}{{ variant.featured_image | image_url: width: 720 }} 720w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 940 -%}{{ variant.featured_image | image_url: width: 940 }} 940w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 1066 -%}{{ variant.featured_image | image_url: width: 1066 }} 1066w,{%- endif -%}
                                                            {{ variant.featured_image | image_url }} {{ variant.featured_image.width }}w"
                                                    {% endif %}
                                            >
                                                <div class="sp-product-color"
                                                     style="--sp-product-card-color-swatches-color: {{ color[1] }};">
                                                </div>
                                                {% if show_tooltip_color_swatch %}
                                                    <span class="sp-tooltip">{{ color[0] | strip }}</span>
                                                {% endif %}
                                            </div>
                                        {% endif %}
                                        {% assign setFirstVariant = true %}
                                    {% endunless %}
                                {% endif %}
                            {% endfor %}
                        {%- endif -%}
                    {%- endfor -%}
                {% endfor %}
            {% endif %}
        {%- endfor -%}
        {% if numItem > max_color_swatches %}
            <div class="sp-product-color-swatch-item color-none">
                <div class="sp-text">{{ numItem | minus: max_color_swatches | prepend: '+' }}</div>
            </div>
        {% endif %}
    </div>
{% endif %}
            </div>
        </div>
    </div>
{% else %}
    {% if placeholder_product %}
        <div class="sp-card sp-product-card"
             data-style="{{ product_card_style | default: "style-1" }}"
             data-content-alignment="{{ product_card_content_alignment }}"
             data-box-shadow="{{ product_card_box_shadow }}"
        >
            <div class="sp-card-inner">
                <div class="sp-card-media sp-product-card-media {% if product_card_show_second_image %}has-second-image{% endif %}">
                    <div class="sp-product-card-badge" data-alignment="vertical">
                        {% if product_card_show_sold_out_badge %}
                            <span class="sp-badge sp-badge--sold-out">{{ 'shinepages.product.sold_out' | t }}</span>
                        {% endif %}
                        {% if product_card_show_discount_badge %}
                            {% assign save = 100.00 | money %}
                            {% if currency_code_enabled %}
                                {% assign save = 100.00 | money_with_currency %}
                            {% endif %}
                            {% if product_card_discount_badge_type == 'percent' %}
                                {% assign save = "10%" %}
                            {% endif %}
                            <span class="sp-badge sp-badge--on-sale">{{ 'shinepages.product.sale_html' | t: price: save }}</span>
                        {% endif %}
                    </div>
                    <a href="#.">
                        {{ 'product-1' | placeholder_svg_tag: 'sp-placeholder-product-1-light' }}
                        {% if product_card_show_second_image %}
                            {{ 'product-2' | placeholder_svg_tag: 'sp-placeholder-product-1-light' }}
                        {% endif %}
                    </a>
                </div>
                <div class="sp-card-information sp-product-card-information">
                    {% if product_card_show_vendor %}
                        <div class="sp-product-card-vendor">shinepages</div>
                    {% endif %}
                    <h3 class="sp-product-card-heading">
                        <a href="#.">Example Product Title</a>
                    </h3>
                    {% if product_card_show_rating %}
                        {% if product_card_show_rating and product.metafields.reviews.rating.value != blank %}
    <div class="sp-product-card-rating">
        {% liquid
            assign rating_decimal = 0
            assign decimal = product.metafields.reviews.rating.value.rating | modulo: 1
            if decimal >= 0.3 and decimal <= 0.7
                assign rating_decimal = 0.5
            elsif decimal > 0.7
                assign rating_decimal = 1
            endif
        %}
        <div class="sp-rating" role="img">
            <span aria-hidden="true" class="sp-rating-star"
                  style="--sp-rating: {{ product.metafields.reviews.rating.value.rating | floor }}; --sp-rating-max: {{ product.metafields.reviews.rating.value.scale_max }}; --sp-rating-decimal: {{ rating_decimal }};"></span>
            {% if product_card_show_rating_number %}
                <span class="sp-rating-number">({{ product.metafields.reviews.rating.value.rating | floor }})</span>
                {% endif %}
        </div>
    </div>
{%- endif -%}
                    {% endif %}

                    {% assign use_variant = true %}
                    {% assign show_compare_at_price = true %}
                    {% assign currency_code_enabled = true %}
                    {% comment %}
  Renders a list of product's price (regular, sale)

  Accepts:
  - product: {Object} Product Liquid object (optional)
  - use_variant: {Boolean} Renders selected or first variant price instead of overall product pricing (optional)
  - price_class: {String} Adds a price class to the price element (optional)
  - show_compare_at_price: {Boolean} Renders the compare at price if the product matches the condition (optional)
  Usage:
  {% render 'price', product: product %}
{% endcomment %}
{% if product_card_show_price %}
    {%- liquid
        if use_first_variant_product
            assign target = product.selected_or_first_available_variant
        else
            assign target = product
        endif

        assign compare_at_price = target.compare_at_price
        assign price = target.price | default: 1999
        assign available = target.available | default: false
        assign money_price = price | money
        if product_card_show_currency_code
            assign money_price = price | money_with_currency
        endif
    -%}

    <div class="sp-product-card-price
    {%- if available == false %} price-sold-out{% endif -%}
    {%- if compare_at_price > price and product.quantity_price_breaks_configured? != true %} price-on-sale{% endif -%}
    {%- if compare_at_price > price and product.quantity_price_breaks_configured? %} volume-pricing-sale-badge{% endif -%}
    {%- if product.price_varies == false and product.compare_at_price_varies %} price-no-compare{% endif -%}
  ">
        <span class="price-item price-item--sale price-item--last">
            {% if product_card_show_currency_code %}
                {{ price | money_with_currency }}
            {% else %}
                {{ price | money }}
            {% endif %}
        </span>
        {%- if show_compare_at_price and compare_at_price > 0 %}
            <span class="price-item price-item--regular">
            {% if product_card_show_currency_code %}
                {{ compare_at_price | money_with_currency }}
            {% else %}
                {{ compare_at_price | money }}
            {% endif %}
        </span>
        {%- endif -%}
    </div>
{% endif %}


                    {% if product_card_show_color_swatches %}
                        {% assign variants = product.variants %}
{% if product_card_show_color_swatches and variants %}
    {% assign colorSwatcheKey = shop.metaobjects['app--40489222145--shine_pages_settings']['shine_pages_settings'].colorSwatchKey.value | downcase %}
    {% assign colorSwatches = shop.metaobjects['app--40489222145--shine_pages_settings']['shine_pages_settings'].colorSwatches.value %}
    {% assign hasVariants = "" %}
    <div class="sp-product-color-swatches" data-alignment="{{ product_card_content_alignment }}"
         data-style="{{ product_card_color_swatch_style }}" data-has-change-image="1">
        {% assign setFirstVariant = false %}
        {% assign numItem = 0 %}
        {%- for option in product.options_with_values -%}
            {%- assign downcased_option = option.name | downcase -%}
            {%- if downcased_option == 'color' or downcased_option == 'colour' or downcased_option == 'couleur' or downcased_option == colorSwatcheKey -%}
                {%- assign variant_option = 'option' | append: forloop.index -%}

                {%- for value in option.values -%}
                    {%- assign downcased_value = value | downcase -%}
                    {%- capture color_id -%}{{ section.id }}-{{ product.id }}-{% increment color_index %}{%- endcapture -%}

                    {%- for variant in product.variants -%}
                        {%- if variant[variant_option] == value -%}
                            {% for color in colorSwatches %}
                                {% if value == color[0] %}
                                    {% assign color_check = color[0] | append: ',' %}
                                    {% unless hasVariants contains color_check %}
                                        {% assign numItem = numItem | plus: 1 %}
                                        {% assign hasVariants = hasVariants | append: color[0] | append: ',' %}
                                        {% if numItem <= max_color_swatches %}
                                            <div class="sp-product-color-swatch-item {% if setFirstVariant == false %}active{% endif %}"
                                                 data-plugin="sp-tooltip"
                                                 onclick="spProductCardColorSwatches(this)"
                                                    {% if variant.featured_image %}
                                                        data-featured-image="{{ variant.featured_image | image_url: width: 550 | default: '' }}"
                                                        data-featured-image-srcset="
                                                            {%- if variant.featured_image.width >= 165 -%}{{ variant.featured_image | image_url: width: 165 }} 165w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 360 -%}{{ variant.featured_image | image_url: width: 360 }} 360w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 533 -%}{{ variant.featured_image | image_url: width: 533 }} 533w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 720 -%}{{ variant.featured_image | image_url: width: 720 }} 720w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 940 -%}{{ variant.featured_image | image_url: width: 940 }} 940w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 1066 -%}{{ variant.featured_image | image_url: width: 1066 }} 1066w,{%- endif -%}
                                                            {{ variant.featured_image | image_url }} {{ variant.featured_image.width }}w"
                                                    {% endif %}
                                            >
                                                <div class="sp-product-color"
                                                     style="--sp-product-card-color-swatches-color: {{ color[1] }};">
                                                </div>
                                                {% if show_tooltip_color_swatch %}
                                                    <span class="sp-tooltip">{{ color[0] | strip }}</span>
                                                {% endif %}
                                            </div>
                                        {% endif %}
                                        {% assign setFirstVariant = true %}
                                    {% endunless %}
                                {% endif %}
                            {% endfor %}
                        {%- endif -%}
                    {%- endfor -%}
                {% endfor %}
            {% endif %}
        {%- endfor -%}
        {% if numItem > max_color_swatches %}
            <div class="sp-product-color-swatch-item color-none">
                <div class="sp-text">{{ numItem | minus: max_color_swatches | prepend: '+' }}</div>
            </div>
        {% endif %}
    </div>
{% endif %}
                    {% endif %}
                </div>
            </div>
        </div>
    {% endif %}
{% endif %}
                                                        </div>
                                                    {% endif %}
                                                    {% if setting.product_4 != null %}
                                                        {% assign product = setting.product_4 %}
                                                        <div class="sp-shop-the-look-product-item"
                                                             data-hotspot-id="sp-shop-the-look-product-item-4-{{ product.id }}-{{ block.id }}">
                                                            {% if product and product != empty %}
    {% capture productData %}
        {% assign onSale = false %}
        {% assign inventory = 0 %}
        {
        "id":{{- product.id -}},
        "available":{{- product.available | default: false -}},
        "title":{{- product.title | json -}},
        "handle":{{- product.handle | json -}},
        "url":"{{- product.url -}}",
        "type":{{- product.type | json -}},
        "price":{{- product.price | default: 0 -}},
        "price_max":{{- product.price_max | default: 0 -}},
        "price_min":{{- product.price_min | default: 0 -}},
        "compare_at_price":{{- product.compare_at_price | default: 0 -}},
        "compare_at_price_max": {{- product.compare_at_price_max | default: 0 -}},
        "compare_at_price_min": {{- product.compare_at_price_min | default: 0 -}},
        "price_varies": {{- product.price_varies | default: false -}},
        "compare_at_price_varies": {{- product.compare_at_price_varies | default: false -}},
        "created_at":"{{- product.created_at | date: "%s%L" -}}",
        "published_at":"{{- product.published_at | date: "%s%L" -}}",
        "tags": [{% for tag in product.tags %}{%- unless forloop.first -%},{%- endunless -%}{{- tag | json -}}{% endfor %}],
        "vendor": {{- product.vendor | json -}},
        "featured_image":"{{- product.featured_image -}}",
        "featured_media":"{% if product.featured_media %}{{- product.featured_media | image_url: width: 450 -}}{% endif %}",
        "images":[
        {%- for image in product.images -%}{%- unless forloop.first -%},
        {%- endunless -%}{"id":{{- image.id -}},
            "src":{{- image.src | json -}}}
        {%- endfor -%}],
        "collections":{{- product.collections | map: 'id' | json -}},
        {%- assign selectedVariant = product.selected_or_first_available_variant -%}
        {% assign sale_amount = 0 %}
        {% assign sale_percent = 0 %}
        {% if selectedVariant.price and selectedVariant.compare_at_price %}
            {% assign sale_amount = selectedVariant.compare_at_price | minus: selectedVariant.price %}
            {% if sale_amount > 0 %}
                {% assign divisor = selectedVariant.compare_at_price | times: 1.0 %}
                {% assign sale_percent = selectedVariant.compare_at_price | minus: selectedVariant.price | divided_by: divisor | times: 100.0 | round %}
            {% endif %}
        {% endif %}
        "variant": {
        "id": "{{ selectedVariant.id }}",
        "title": {{- selectedVariant.title | json -}},
        "available": {{- selectedVariant.available | default: false -}},
        "inventory_management": "{{- selectedVariant.inventory_management -}}",
        "inventory_policy": "{{- selectedVariant.inventory_policy -}}",
        "inventory_quantity": {{- selectedVariant.inventory_quantity | default: 0 -}},
        "price": {{- selectedVariant.price | default: 0 -}},
        "compare_at_price": {{- selectedVariant.compare_at_price | default: 0 -}},
        "unit_price": "{{- selectedVariant.unit_price -}}",
        "weight": "{{- selectedVariant.weight -}}",
        "weight_in_unit": "{{- selectedVariant.weight_in_unit -}}",
        "weight_unit": "{{- selectedVariant.weight_unit -}}",
        "sale_amount": {{- sale_amount | money | json -}},
        "sale_percent": {{- sale_percent | json -}}
        },
        "variants":{
        {%- for variant in product.variants -%}
            {%- unless forloop.first -%},{%- endunless -%}
            {% if variant.compare_at_price > variant.price %}
                {% assign onSale = true %}
            {% endif %}
            {% if variant.available and variant.inventory_management != null and variant.inventory_management != '' %}
                {% if variant.inventory_policy == 'deny' and variant.inventory_quantity > 0 %}
                    {% assign inventory = inventory | plus: variant.inventory_quantity %}
                {% elsif variant.inventory_policy == 'continue' %}
                    {% assign inventory = -1 %}
                {% endif %}
            {% elsif variant.available and variant.inventory_management == null %}
                {% assign inventory = -1 %}
            {% endif %}
            {% assign sale_amount = 0 %}
            {% assign sale_percent = 0 %}
            {% if variant.price and variant.compare_at_price %}
                {% assign sale_amount = variant.compare_at_price | minus: variant.price %}
                {% if sale_amount > 0 %}
                    {% assign divisor = variant.compare_at_price | times: 1.0 %}
                    {% assign sale_percent = variant.compare_at_price | minus: variant.price | divided_by: divisor | times: 100.0 | round %}
                {% endif %}
            {% endif %}
            "{{- variant.id -}}": {
            "id":{{- variant.id -}},
            "title":{{- variant.title | json }},
            "name":{{- variant.name | json }},
            "price":{{- variant.price | default: 0 -}},
            "compare_at_price":{{- variant.compare_at_price | default: 0 -}},
            "available":{{- variant.available | default: false -}},
            "sku":{{- variant.sku | json -}},
            "weight":"{{- variant.weight -}}",
            "weight_unit": "{{- variant.weight_unit -}}",
            "inventory_management": "{{- variant.inventory_management -}}",
            "inventory_policy": "{{- variant.inventory_policy -}}",
            "inventory_quantity": {{- variant.inventory_quantity | default: 0 -}},
            "selected": {%- if variant.selected -%}  {{- variant.selected -}} {%- elsif product.selected_or_first_available_variant.id == variant.id -%} true  {%- else -%} false {%- endif -%},
            "sale_amount": {{- sale_amount | money | json -}},
            "sale_percent": {{- sale_percent | json -}}
            }
        {%- endfor -%}
        },
        "on_sale":{{- onSale -}},
        "inventory":{{- inventory -}},
        "options":{{- product.options_with_values | json -}},
        "has_only_default_variant": {{ product.has_only_default_variant | default: false }}
        }
    {% endcapture %}
    <div class="sp-card sp-product-card"
         data-style="{{ product_card_style | default: "style-1" }}"
         data-content-alignment="{{ product_card_content_alignment }}"
         data-box-shadow="{{ product_card_box_shadow }}"
    >
        <div data-app="shine-trust-app" data-product="{{- productData | base64_encode -}}"></div>
        <div class="sp-card-inner">
            <div class="sp-card-media sp-product-card-media {% if product.media[1] != null and product_card_show_second_image %}has-second-image{% endif %}">
                <div class="sp-product-card-badge" data-alignment="vertical">
                    {% if product_card_show_discount_badge and product.compare_at_price > product.price %}
                        {% assign save = product.compare_at_price | minus: product.price | money %}
                        {% if product_card_show_currency_code %}
                            {% assign save = product.compare_at_price | minus: product.price | money_with_currency %}
                        {% endif %}
                        {% if product_card_discount_badge_type == 'percent' %}
                            {% assign save = product.compare_at_price | minus: product.price | times: 1.0 | divided_by: product.compare_at_price | times: 100 | round: 0 | append: "%" %}
                        {% endif %}
                        <span class="sp-badge sp-badge--on-sale">{{ 'shinepages.product.sale_html' | t: price: save }}</span>
                    {% endif %}
                    {% if product_card_show_sold_out_badge and product.available == false %}
                        <span class="sp-badge sp-badge--sold-out">{{ 'shinepages.product.sold_out' | t }}</span>
                    {% endif %}
                </div>
                <a class="sp-link" href="{{ product.url }}">
                    <img srcset="
                  {%- if product.featured_media.width >= 165 -%}{{ product.featured_media | image_url: width: 165 }} 165w,{%- endif -%}
                  {%- if product.featured_media.width >= 360 -%}{{ product.featured_media | image_url: width: 360 }} 360w,{%- endif -%}
                  {%- if product.featured_media.width >= 533 -%}{{ product.featured_media | image_url: width: 533 }} 533w,{%- endif -%}
                  {%- if product.featured_media.width >= 720 -%}{{ product.featured_media | image_url: width: 720 }} 720w,{%- endif -%}
                  {%- if product.featured_media.width >= 940 -%}{{ product.featured_media | image_url: width: 940 }} 940w,{%- endif -%}
                  {%- if product.featured_media.width >= 1066 -%}{{ product.featured_media | image_url: width: 1066 }} 1066w,{%- endif -%}
                  {{ product.featured_media | image_url }} {{ product.featured_media.width }}w
                "
                         src="{{ product.featured_media | image_url: width: 550 }}"
                         sizes="(min-width: 1200px) 1200px, (min-width: 990px) 990px, (min-width: 750px) 750px, 650px)"
                         alt="{{ product.featured_media.alt | escape }}"
                         width="{{ product.featured_media.width }}"
                         height="{{ product.featured_media.height }}"
                         data-change-variant-image="{{ product.featured_media | image_url: width: 550 }}"
                         data-change-variant-image-set="
                  {%- if product.featured_media.width >= 165 -%}{{ product.featured_media | image_url: width: 165 }} 165w,{%- endif -%}
                  {%- if product.featured_media.width >= 360 -%}{{ product.featured_media | image_url: width: 360 }} 360w,{%- endif -%}
                  {%- if product.featured_media.width >= 533 -%}{{ product.featured_media | image_url: width: 533 }} 533w,{%- endif -%}
                  {%- if product.featured_media.width >= 720 -%}{{ product.featured_media | image_url: width: 720 }} 720w,{%- endif -%}
                  {%- if product.featured_media.width >= 940 -%}{{ product.featured_media | image_url: width: 940 }} 940w,{%- endif -%}
                  {%- if product.featured_media.width >= 1066 -%}{{ product.featured_media | image_url: width: 1066 }} 1066w,{%- endif -%}
                  {{ product.featured_media | image_url }} {{ product.featured_media.width }}w
                "
                    >
                    {%- if product.media[1] != null and product_card_show_second_image -%}
                        <img
                                srcset="
                    {%- if product.media[1].width >= 165 -%}{{ product.media[1] | image_url: width: 165 }} 165w,{%- endif -%}
                    {%- if product.media[1].width >= 360 -%}{{ product.media[1] | image_url: width: 360 }} 360w,{%- endif -%}
                    {%- if product.media[1].width >= 533 -%}{{ product.media[1] | image_url: width: 533 }} 533w,{%- endif -%}
                    {%- if product.media[1].width >= 720 -%}{{ product.media[1] | image_url: width: 720 }} 720w,{%- endif -%}
                    {%- if product.media[1].width >= 940 -%}{{ product.media[1] | image_url: width: 940 }} 940w,{%- endif -%}
                    {%- if product.media[1].width >= 1066 -%}{{ product.media[1] | image_url: width: 1066 }} 1066w,{%- endif -%}
                    {{ product.media[1] | image_url }} {{ product.media[1].width }}w
                  "
                                src="{{ product.media[1] | image_url: width: 1080 }}"
                                sizes="(min-width: {{ settings.page_width }}px) {{ settings.page_width | minus: 130 | divided_by: 4 }}px, (min-width: 990px) calc((100vw - 130px) / 4), (min-width: 750px) calc((100vw - 120px) / 3), calc((100vw - 35px) / 2)"
                                alt=""
                                width="{{ product.media[1].width }}"
                                height="{{ product.media[1].height }}"
                        >
                    {%- endif -%}
                </a>
            </div>
            <div class="sp-card-information sp-product-card-information sp-text-alignment-{{ text_alignment }}">
                {% if product_card_show_vendor %}
                    <div class="sp-product-card-vendor">{{ product.vendor }}</div>
                {% endif %}
                <h3 class="sp-product-card-heading">
                    <a class="sp-link" href="{{ product.url }}">{{ product.title | escape }}</a>
                </h3>
                {% if product_card_show_rating and product.metafields.reviews.rating.value != blank %}
    <div class="sp-product-card-rating">
        {% liquid
            assign rating_decimal = 0
            assign decimal = product.metafields.reviews.rating.value.rating | modulo: 1
            if decimal >= 0.3 and decimal <= 0.7
                assign rating_decimal = 0.5
            elsif decimal > 0.7
                assign rating_decimal = 1
            endif
        %}
        <div class="sp-rating" role="img">
            <span aria-hidden="true" class="sp-rating-star"
                  style="--sp-rating: {{ product.metafields.reviews.rating.value.rating | floor }}; --sp-rating-max: {{ product.metafields.reviews.rating.value.scale_max }}; --sp-rating-decimal: {{ rating_decimal }};"></span>
            {% if product_card_show_rating_number %}
                <span class="sp-rating-number">({{ product.metafields.reviews.rating.value.rating | floor }})</span>
                {% endif %}
        </div>
    </div>
{%- endif -%}
                {% comment %}
  Renders a list of product's price (regular, sale)

  Accepts:
  - product: {Object} Product Liquid object (optional)
  - use_variant: {Boolean} Renders selected or first variant price instead of overall product pricing (optional)
  - price_class: {String} Adds a price class to the price element (optional)
  - show_compare_at_price: {Boolean} Renders the compare at price if the product matches the condition (optional)
  Usage:
  {% render 'price', product: product %}
{% endcomment %}
{% if product_card_show_price %}
    {%- liquid
        if use_first_variant_product
            assign target = product.selected_or_first_available_variant
        else
            assign target = product
        endif

        assign compare_at_price = target.compare_at_price
        assign price = target.price | default: 1999
        assign available = target.available | default: false
        assign money_price = price | money
        if product_card_show_currency_code
            assign money_price = price | money_with_currency
        endif
    -%}

    <div class="sp-product-card-price
    {%- if available == false %} price-sold-out{% endif -%}
    {%- if compare_at_price > price and product.quantity_price_breaks_configured? != true %} price-on-sale{% endif -%}
    {%- if compare_at_price > price and product.quantity_price_breaks_configured? %} volume-pricing-sale-badge{% endif -%}
    {%- if product.price_varies == false and product.compare_at_price_varies %} price-no-compare{% endif -%}
  ">
        <span class="price-item price-item--sale price-item--last">
            {% if product_card_show_currency_code %}
                {{ price | money_with_currency }}
            {% else %}
                {{ price | money }}
            {% endif %}
        </span>
        {%- if show_compare_at_price and compare_at_price > 0 %}
            <span class="price-item price-item--regular">
            {% if product_card_show_currency_code %}
                {{ compare_at_price | money_with_currency }}
            {% else %}
                {{ compare_at_price | money }}
            {% endif %}
        </span>
        {%- endif -%}
    </div>
{% endif %}

                {% assign variants = product.variants %}
{% if product_card_show_color_swatches and variants %}
    {% assign colorSwatcheKey = shop.metaobjects['app--40489222145--shine_pages_settings']['shine_pages_settings'].colorSwatchKey.value | downcase %}
    {% assign colorSwatches = shop.metaobjects['app--40489222145--shine_pages_settings']['shine_pages_settings'].colorSwatches.value %}
    {% assign hasVariants = "" %}
    <div class="sp-product-color-swatches" data-alignment="{{ product_card_content_alignment }}"
         data-style="{{ product_card_color_swatch_style }}" data-has-change-image="1">
        {% assign setFirstVariant = false %}
        {% assign numItem = 0 %}
        {%- for option in product.options_with_values -%}
            {%- assign downcased_option = option.name | downcase -%}
            {%- if downcased_option == 'color' or downcased_option == 'colour' or downcased_option == 'couleur' or downcased_option == colorSwatcheKey -%}
                {%- assign variant_option = 'option' | append: forloop.index -%}

                {%- for value in option.values -%}
                    {%- assign downcased_value = value | downcase -%}
                    {%- capture color_id -%}{{ section.id }}-{{ product.id }}-{% increment color_index %}{%- endcapture -%}

                    {%- for variant in product.variants -%}
                        {%- if variant[variant_option] == value -%}
                            {% for color in colorSwatches %}
                                {% if value == color[0] %}
                                    {% assign color_check = color[0] | append: ',' %}
                                    {% unless hasVariants contains color_check %}
                                        {% assign numItem = numItem | plus: 1 %}
                                        {% assign hasVariants = hasVariants | append: color[0] | append: ',' %}
                                        {% if numItem <= max_color_swatches %}
                                            <div class="sp-product-color-swatch-item {% if setFirstVariant == false %}active{% endif %}"
                                                 data-plugin="sp-tooltip"
                                                 onclick="spProductCardColorSwatches(this)"
                                                    {% if variant.featured_image %}
                                                        data-featured-image="{{ variant.featured_image | image_url: width: 550 | default: '' }}"
                                                        data-featured-image-srcset="
                                                            {%- if variant.featured_image.width >= 165 -%}{{ variant.featured_image | image_url: width: 165 }} 165w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 360 -%}{{ variant.featured_image | image_url: width: 360 }} 360w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 533 -%}{{ variant.featured_image | image_url: width: 533 }} 533w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 720 -%}{{ variant.featured_image | image_url: width: 720 }} 720w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 940 -%}{{ variant.featured_image | image_url: width: 940 }} 940w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 1066 -%}{{ variant.featured_image | image_url: width: 1066 }} 1066w,{%- endif -%}
                                                            {{ variant.featured_image | image_url }} {{ variant.featured_image.width }}w"
                                                    {% endif %}
                                            >
                                                <div class="sp-product-color"
                                                     style="--sp-product-card-color-swatches-color: {{ color[1] }};">
                                                </div>
                                                {% if show_tooltip_color_swatch %}
                                                    <span class="sp-tooltip">{{ color[0] | strip }}</span>
                                                {% endif %}
                                            </div>
                                        {% endif %}
                                        {% assign setFirstVariant = true %}
                                    {% endunless %}
                                {% endif %}
                            {% endfor %}
                        {%- endif -%}
                    {%- endfor -%}
                {% endfor %}
            {% endif %}
        {%- endfor -%}
        {% if numItem > max_color_swatches %}
            <div class="sp-product-color-swatch-item color-none">
                <div class="sp-text">{{ numItem | minus: max_color_swatches | prepend: '+' }}</div>
            </div>
        {% endif %}
    </div>
{% endif %}
            </div>
        </div>
    </div>
{% else %}
    {% if placeholder_product %}
        <div class="sp-card sp-product-card"
             data-style="{{ product_card_style | default: "style-1" }}"
             data-content-alignment="{{ product_card_content_alignment }}"
             data-box-shadow="{{ product_card_box_shadow }}"
        >
            <div class="sp-card-inner">
                <div class="sp-card-media sp-product-card-media {% if product_card_show_second_image %}has-second-image{% endif %}">
                    <div class="sp-product-card-badge" data-alignment="vertical">
                        {% if product_card_show_sold_out_badge %}
                            <span class="sp-badge sp-badge--sold-out">{{ 'shinepages.product.sold_out' | t }}</span>
                        {% endif %}
                        {% if product_card_show_discount_badge %}
                            {% assign save = 100.00 | money %}
                            {% if currency_code_enabled %}
                                {% assign save = 100.00 | money_with_currency %}
                            {% endif %}
                            {% if product_card_discount_badge_type == 'percent' %}
                                {% assign save = "10%" %}
                            {% endif %}
                            <span class="sp-badge sp-badge--on-sale">{{ 'shinepages.product.sale_html' | t: price: save }}</span>
                        {% endif %}
                    </div>
                    <a href="#.">
                        {{ 'product-1' | placeholder_svg_tag: 'sp-placeholder-product-1-light' }}
                        {% if product_card_show_second_image %}
                            {{ 'product-2' | placeholder_svg_tag: 'sp-placeholder-product-1-light' }}
                        {% endif %}
                    </a>
                </div>
                <div class="sp-card-information sp-product-card-information">
                    {% if product_card_show_vendor %}
                        <div class="sp-product-card-vendor">shinepages</div>
                    {% endif %}
                    <h3 class="sp-product-card-heading">
                        <a href="#.">Example Product Title</a>
                    </h3>
                    {% if product_card_show_rating %}
                        {% if product_card_show_rating and product.metafields.reviews.rating.value != blank %}
    <div class="sp-product-card-rating">
        {% liquid
            assign rating_decimal = 0
            assign decimal = product.metafields.reviews.rating.value.rating | modulo: 1
            if decimal >= 0.3 and decimal <= 0.7
                assign rating_decimal = 0.5
            elsif decimal > 0.7
                assign rating_decimal = 1
            endif
        %}
        <div class="sp-rating" role="img">
            <span aria-hidden="true" class="sp-rating-star"
                  style="--sp-rating: {{ product.metafields.reviews.rating.value.rating | floor }}; --sp-rating-max: {{ product.metafields.reviews.rating.value.scale_max }}; --sp-rating-decimal: {{ rating_decimal }};"></span>
            {% if product_card_show_rating_number %}
                <span class="sp-rating-number">({{ product.metafields.reviews.rating.value.rating | floor }})</span>
                {% endif %}
        </div>
    </div>
{%- endif -%}
                    {% endif %}

                    {% assign use_variant = true %}
                    {% assign show_compare_at_price = true %}
                    {% assign currency_code_enabled = true %}
                    {% comment %}
  Renders a list of product's price (regular, sale)

  Accepts:
  - product: {Object} Product Liquid object (optional)
  - use_variant: {Boolean} Renders selected or first variant price instead of overall product pricing (optional)
  - price_class: {String} Adds a price class to the price element (optional)
  - show_compare_at_price: {Boolean} Renders the compare at price if the product matches the condition (optional)
  Usage:
  {% render 'price', product: product %}
{% endcomment %}
{% if product_card_show_price %}
    {%- liquid
        if use_first_variant_product
            assign target = product.selected_or_first_available_variant
        else
            assign target = product
        endif

        assign compare_at_price = target.compare_at_price
        assign price = target.price | default: 1999
        assign available = target.available | default: false
        assign money_price = price | money
        if product_card_show_currency_code
            assign money_price = price | money_with_currency
        endif
    -%}

    <div class="sp-product-card-price
    {%- if available == false %} price-sold-out{% endif -%}
    {%- if compare_at_price > price and product.quantity_price_breaks_configured? != true %} price-on-sale{% endif -%}
    {%- if compare_at_price > price and product.quantity_price_breaks_configured? %} volume-pricing-sale-badge{% endif -%}
    {%- if product.price_varies == false and product.compare_at_price_varies %} price-no-compare{% endif -%}
  ">
        <span class="price-item price-item--sale price-item--last">
            {% if product_card_show_currency_code %}
                {{ price | money_with_currency }}
            {% else %}
                {{ price | money }}
            {% endif %}
        </span>
        {%- if show_compare_at_price and compare_at_price > 0 %}
            <span class="price-item price-item--regular">
            {% if product_card_show_currency_code %}
                {{ compare_at_price | money_with_currency }}
            {% else %}
                {{ compare_at_price | money }}
            {% endif %}
        </span>
        {%- endif -%}
    </div>
{% endif %}


                    {% if product_card_show_color_swatches %}
                        {% assign variants = product.variants %}
{% if product_card_show_color_swatches and variants %}
    {% assign colorSwatcheKey = shop.metaobjects['app--40489222145--shine_pages_settings']['shine_pages_settings'].colorSwatchKey.value | downcase %}
    {% assign colorSwatches = shop.metaobjects['app--40489222145--shine_pages_settings']['shine_pages_settings'].colorSwatches.value %}
    {% assign hasVariants = "" %}
    <div class="sp-product-color-swatches" data-alignment="{{ product_card_content_alignment }}"
         data-style="{{ product_card_color_swatch_style }}" data-has-change-image="1">
        {% assign setFirstVariant = false %}
        {% assign numItem = 0 %}
        {%- for option in product.options_with_values -%}
            {%- assign downcased_option = option.name | downcase -%}
            {%- if downcased_option == 'color' or downcased_option == 'colour' or downcased_option == 'couleur' or downcased_option == colorSwatcheKey -%}
                {%- assign variant_option = 'option' | append: forloop.index -%}

                {%- for value in option.values -%}
                    {%- assign downcased_value = value | downcase -%}
                    {%- capture color_id -%}{{ section.id }}-{{ product.id }}-{% increment color_index %}{%- endcapture -%}

                    {%- for variant in product.variants -%}
                        {%- if variant[variant_option] == value -%}
                            {% for color in colorSwatches %}
                                {% if value == color[0] %}
                                    {% assign color_check = color[0] | append: ',' %}
                                    {% unless hasVariants contains color_check %}
                                        {% assign numItem = numItem | plus: 1 %}
                                        {% assign hasVariants = hasVariants | append: color[0] | append: ',' %}
                                        {% if numItem <= max_color_swatches %}
                                            <div class="sp-product-color-swatch-item {% if setFirstVariant == false %}active{% endif %}"
                                                 data-plugin="sp-tooltip"
                                                 onclick="spProductCardColorSwatches(this)"
                                                    {% if variant.featured_image %}
                                                        data-featured-image="{{ variant.featured_image | image_url: width: 550 | default: '' }}"
                                                        data-featured-image-srcset="
                                                            {%- if variant.featured_image.width >= 165 -%}{{ variant.featured_image | image_url: width: 165 }} 165w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 360 -%}{{ variant.featured_image | image_url: width: 360 }} 360w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 533 -%}{{ variant.featured_image | image_url: width: 533 }} 533w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 720 -%}{{ variant.featured_image | image_url: width: 720 }} 720w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 940 -%}{{ variant.featured_image | image_url: width: 940 }} 940w,{%- endif -%}
                                                            {%- if variant.featured_image.width >= 1066 -%}{{ variant.featured_image | image_url: width: 1066 }} 1066w,{%- endif -%}
                                                            {{ variant.featured_image | image_url }} {{ variant.featured_image.width }}w"
                                                    {% endif %}
                                            >
                                                <div class="sp-product-color"
                                                     style="--sp-product-card-color-swatches-color: {{ color[1] }};">
                                                </div>
                                                {% if show_tooltip_color_swatch %}
                                                    <span class="sp-tooltip">{{ color[0] | strip }}</span>
                                                {% endif %}
                                            </div>
                                        {% endif %}
                                        {% assign setFirstVariant = true %}
                                    {% endunless %}
                                {% endif %}
                            {% endfor %}
                        {%- endif -%}
                    {%- endfor -%}
                {% endfor %}
            {% endif %}
        {%- endfor -%}
        {% if numItem > max_color_swatches %}
            <div class="sp-product-color-swatch-item color-none">
                <div class="sp-text">{{ numItem | minus: max_color_swatches | prepend: '+' }}</div>
            </div>
        {% endif %}
    </div>
{% endif %}
                    {% endif %}
                </div>
            </div>
        </div>
    {% endif %}
{% endif %}
                                                        </div>
                                                    {% endif %}
                                                {% endif %}
                                            </div>
                                            <div class="sp-shop-the-look-content-pagination">
                                                <div class="sp-dots-wrapper">
                                                    <div class="sp-dots-inner">
                                                        <ol class="flickity-page-dots">
                                                            {% if setting.product == null and setting.product_2 == null and setting.product_3 == null and setting.product_4 == null %}
                                                                {% assign product = null %}
                                                                {% for i in (1..3) %}
                                                                    <li class="dot" onclick="spChangeHotSpot(this)"
                                                                        data-modal-id="sp-modal-shop-the-look-{{ block.id }}"
                                                                        data-hotspot-id="sp-shop-the-look-product-item-1-{{ i }}-{{ block.id }}">
                                                                        <span class="dot-overlay"></span></li>
                                                                {% endfor %}
                                                            {% else %}
                                                                {% if setting.product != null %}
                                                                    <li class="dot" onclick="spChangeHotSpot(this)"
                                                                        data-modal-id="sp-modal-shop-the-look-{{ block.id }}"
                                                                        data-hotspot-id="sp-shop-the-look-product-item-1-{{ setting.product.id }}-{{ block.id }}">
                                                                        <span class="dot-overlay"></span></li>
                                                                {% endif %}
                                                                {% if setting.product_2 != null %}
                                                                    <li class="dot" onclick="spChangeHotSpot(this)"
                                                                        data-modal-id="sp-modal-shop-the-look-{{ block.id }}"
                                                                        data-hotspot-id="sp-shop-the-look-product-item-2-{{ setting.product_2.id }}-{{ block.id }}">
                                                                        <span class="dot-overlay"></span></li>
                                                                {% endif %}
                                                                {% if setting.product_3 != null %}
                                                                    <li class="dot" onclick="spChangeHotSpot(this)"
                                                                        data-modal-id="sp-modal-shop-the-look-{{ block.id }}"
                                                                        data-hotspot-id="sp-shop-the-look-product-item-3-{{ setting.product_3.id }}-{{ block.id }}">
                                                                        <span class="dot-overlay"></span></li>
                                                                {% endif %}
                                                                {% if setting.product_4 != null %}
                                                                    <li class="dot" onclick="spChangeHotSpot(this)"
                                                                        data-modal-id="sp-modal-shop-the-look-{{ block.id }}"
                                                                        data-hotspot-id="sp-shop-the-look-product-item-4-{{ setting.product_4.id }}-{{ block.id }}">
                                                                        <span class="dot-overlay"></span></li>
                                                                {% endif %}
                                                            {% endif %}
                                                        </ol>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                {% assign slideIndex = slideIndex | plus: 1 %}
                            {% endif %}
                        {% endfor %}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% schema %}
{
	"name": "🧩 Shop the look 1.3",
	"tag": "section",
	"class": "sp-slideshow-1",
	"settings": [
		{
			"type": "header",
			"content": "Shop the look setting"
		},
		{
			"type": "select",
			"id": "image_position",
			"label": "Image position",
			"options": [
				{
					"label": "Left",
					"value": "left"
				},
				{
					"label": "Right",
					"value": "right"
				}
			],
			"default": "right"
		},
		{
			"type": "range",
			"id": "image_border_radius",
			"min": 0,
			"max": 60,
			"step": 1,
			"unit": "px",
			"label": "Image border radius",
			"default": 0
		},
		{
			"type": "range",
			"id": "image_border_size",
			"min": 0,
			"max": 10,
			"step": 1,
			"unit": "px",
			"label": "Image border size",
			"default": 0
		},
		{
			"type": "checkbox",
			"id": "box_shadow",
			"label": "Box shadow",
			"default": false
		},
		{
			"type": "header",
			"content": "Product card"
		},
		{
			"type": "select",
			"id": "product_card_content_alignment",
			"label": "Content alignment",
			"default": "center",
			"options": [
				{
					"label": "Left",
					"value": "left"
				},
				{
					"label": "Center",
					"value": "center"
				},
				{
					"label": "Right",
					"value": "right"
				}
			]
		},
		{
			"type": "select",
			"id": "product_card_style",
			"label": "Style",
			"options": [
				{
					"label": "Standard",
					"value": "standard"
				},
				{
					"label": "Card",
					"value": "card"
				}
			],
			"default": "standard"
		},
		{
			"type": "range",
			"id": "product_card_image_ratio",
			"min": 0.3,
			"max": 2,
			"step": 0.1,
			"default": 0.8,
			"unit": ":1",
			"label": "Image ratio"
		},
		{
			"type": "range",
			"id": "product_card_border_radius",
			"min": 0,
			"max": 60,
			"step": 1,
			"unit": "px",
			"label": "Card border radius",
			"default": 0
		},
		{
			"type": "range",
			"id": "product_card_badge_border_radius",
			"min": 0,
			"max": 60,
			"step": 1,
			"unit": "px",
			"label": "Badge border radius",
			"default": 0
		},
		{
			"type": "range",
			"id": "product_card_border_size",
			"min": 0,
			"max": 10,
			"step": 1,
			"unit": "px",
			"label": "Border size",
			"info": "Apply only card style",
			"default": 0
		},
		{
			"type": "checkbox",
			"id": "product_card_box_shadow",
			"label": "Box shadow",
			"info": "Apply only card style",
			"default": false
		},
		{
			"type": "checkbox",
			"id": "product_card_show_second_image",
			"label": "Show second image on hover",
			"default": true
		},
		{
			"type": "checkbox",
			"id": "product_card_show_vendor",
			"label": "Show vendor",
			"default": true
		},
		{
			"type": "checkbox",
			"id": "product_card_show_price",
			"label": "Show price",
			"default": true
		},
		{
			"type": "checkbox",
			"id": "product_card_show_discount_badge",
			"label": "Show discount badge",
			"default": true
		},
		{
			"type": "checkbox",
			"id": "product_card_show_sold_out_badge",
			"label": "Show sold out badge",
			"default": true
		},
		{
			"type": "select",
			"id": "product_card_discount_badge_type",
			"label": "Discount badge type",
			"options": [
				{
					"label": "Sale amount",
					"value": "amount"
				},
				{
					"label": "Sale percent",
					"value": "percent"
				}
			],
			"default": "amount"
		},
		{
			"type": "checkbox",
			"id": "product_card_show_currency_code",
			"label": "Show currency codes",
			"info": "Always show currency codes. Example: $1.00 USD",
			"default": false
		},
		{
			"type": "checkbox",
			"id": "product_card_show_rating",
			"label": "Show rating",
			"info": "To display a rating, add a product rating app. [Learn More](https://apps.shopify.com/judgeme)",
			"default": false
		},
		{
			"type": "checkbox",
			"id": "product_card_show_color_swatches",
			"label": "Show color swatch",
			"default": false
		},
		{
			"type": "select",
			"id": "product_card_color_swatch_style",
			"label": "Color swatch style",
			"options": [
				{
					"label": "Circle",
					"value": "circle"
				},
				{
					"label": "Square",
					"value": "square"
				}
			],
			"default": "circle"
		},
		{
			"type": "header",
			"content": "Pagination"
		},
		{
			"type": "select",
			"id": "pagination_position",
			"label": "Pagination position",
			"options": [
				{
					"label": "Bottom left",
					"value": "bottom_left"
				},
				{
					"label": "Bottom center",
					"value": "bottom_center"
				},
				{
					"label": "Bottom right",
					"value": "bottom_right"
				}
			],
			"default": "bottom_center"
		},
		{
			"type": "select",
			"id": "pagination_style",
			"label": "Pagination style",
			"options": [
				{
					"label": "Dot",
					"value": "circle"
				},
				{
					"label": "Line",
					"value": "line"
				}
			],
			"default": "line"
		},
		{
			"type": "checkbox",
			"id": "show_background_pagination",
			"label": "Show background pagination",
			"default": false
		},
		{
			"type": "range",
			"id": "pagination_border_radius",
			"min": 0,
			"max": 30,
			"step": 1,
			"unit": "px",
			"label": "Border radius",
			"default": 20
		},
		{
			"type": "header",
			"content": "Navigation"
		},
		{
			"type": "checkbox",
			"id": "navigation_box_shadow",
			"label": "Box shadow",
			"default": true
		},
		{
			"type": "select",
			"id": "navigation_style",
			"label": "Navigation style",
			"options": [
				{
					"label": "Button fill",
					"value": "fill"
				},
				{
					"label": "Button outline",
					"value": "outline"
				},
				{
					"label": "Only icon",
					"value": "icon"
				}
			],
			"default": "fill"
		},
		{
			"type": "range",
			"id": "navigation_button_scale",
			"min": 50,
			"max": 150,
			"step": 10,
			"unit": "%",
			"label": "Button scale",
			"default": 100
		},
		{
			"type": "range",
			"id": "navigation_button_radius",
			"min": 0,
			"max": 60,
			"step": 1,
			"unit": "px",
			"label": "Button border radius",
			"default": 40
		},
		{
			"type": "range",
			"id": "navigation_button_border_width",
			"min": 0.5,
			"max": 5,
			"step": 0.5,
			"unit": "px",
			"label": "Button border size",
			"info": "Apply to button outline style",
			"default": 1
		},
		{
			"type": "header",
			"content": "Section padding"
		},
		{
			"type": "range",
			"id": "section_top_padding",
			"min": 0,
			"max": 150,
			"step": 10,
			"unit": "px",
			"label": "Top padding",
			"default": 100
		},
		{
			"type": "range",
			"id": "section_bottom_padding",
			"min": 0,
			"max": 150,
			"step": 10,
			"unit": "px",
			"label": "Bottom padding",
			"default": 100
		},
		{
			"type": "checkbox",
			"id": "section_border",
			"label": "Separate section with border",
			"default": false
		},
		{
			"type": "header",
			"content": "Effects"
		},
		{
			"type": "checkbox",
			"id": "lazy_load",
			"label": "Lazy load",
			"default": true
		},
		{
			"type": "header",
			"content": "Section color"
		},
		{
			"type": "color",
			"id": "section_background_color",
			"label": "Background",
			"default": "#FFF"
		},
		{
			"type": "color_background",
			"id": "section_background_gradient_color",
			"label": "Background gradient"
		},
		{
			"type": "color",
			"id": "section_text_color",
			"label": "Text",
			"default": "#1e1e1e"
		},
		{
			"type": "header",
			"content": "Shop the look color"
		},
		{
			"type": "color",
			"id": "hotspot_color",
			"label": "Hotspot",
			"default": "#60FFCF"
		},
		{
			"type": "color_background",
			"id": "image_overlay_color",
			"label": "Overlay",
			"default": "linear-gradient(0deg, rgba(0, 0, 0, 0.20) 0%, rgba(0, 0, 0, 0.20) 100%)"
		},
		{
			"type": "color",
			"id": "image_border_color",
			"label": "Border image",
			"default": "#1e1e1e"
		},
		{
			"type": "header",
			"content": "Product card color"
		},
		{
			"type": "color",
			"id": "product_card_text_color",
			"label": "Text",
			"default": "#1e1e1e"
		},
		{
			"type": "color",
			"id": "product_card_price_color",
			"label": "Price",
			"default": "#1e1e1e"
		},
		{
			"type": "color",
			"id": "product_card_price_rating_color",
			"label": "Rating",
			"default": "#1e1e1e"
		},
		{
			"type": "color",
			"id": "product_card_background_color",
			"label": "Background",
			"default": "#FFF"
		},
		{
			"type": "color_background",
			"id": "product_card_background_gradient_color",
			"label": "Background gradient",
			"default": "linear-gradient(0deg, rgba(255, 255, 255, 1) 0%, rgba(255, 255, 255, 1) 100%)"
		},
		{
			"type": "color",
			"id": "product_card_shadow_color",
			"label": "Shadow",
			"default": "#000"
		},
		{
			"type": "color",
			"id": "product_card_border_color",
			"label": "Border"
		},
		{
			"type": "color",
			"id": "product_card_border_thumbnail_color",
			"label": "Border thumbnail"
		},
		{
			"type": "color",
			"id": "product_card_discount_badge_text_color",
			"label": "Discount badge (label)",
			"default": "#FFF"
		},
		{
			"type": "color",
			"id": "product_card_discount_badge_background_color",
			"label": "Discount badge (background)",
			"default": "#C43235"
		},
		{
			"type": "color",
			"id": "product_card_sold_badge_text_color",
			"label": "Sold out badge (label)",
			"default": "#FFF"
		},
		{
			"type": "color",
			"id": "product_card_sold_badge_background_color",
			"label": "Sold out badge (background)",
			"default": "#ACACAC"
		},
		{
			"type": "header",
			"content": "Navigation button color"
		},
		{
			"type": "color",
			"id": "navigation_background",
			"label": "Background",
			"default": "#FFF"
		},
		{
			"type": "color_background",
			"id": "navigation_background_gradient",
			"label": "Background gradient",
			"info": "Background gradient replaces background where possible.",
			"default": "linear-gradient(90deg, rgba(255, 255, 255, 0.4) 2%, rgba(255, 255, 255, 0.4) 100%)"
		},
		{
			"type": "color",
			"id": "navigation_icon",
			"label": "Icon",
			"default": "#1e1e1e"
		},
		{
			"type": "color",
			"id": "navigation_background_shadow",
			"label": "Shadow",
			"default": "#23468F"
		},
		{
			"type": "header",
			"content": "Pagination color"
		},
		{
			"type": "color_background",
			"id": "pagination_background",
			"label": "Background",
			"default": "linear-gradient(90deg, rgba(102, 96, 96, 0.4) 2%, rgba(61, 56, 56, 0.4) 100%)"
		},
		{
			"type": "color",
			"id": "pagination_highlight",
			"label": "Highlight",
			"default": "#868686"
		},
		{
			"type": "header",
			"content": "Typography"
		},
		{
			"type": "checkbox",
			"id": "override_font",
			"label": "Override fonts of the theme",
			"default": true,
			"info": "If you check this option, the fonts of the section will work"
		},
		{
			"type": "font_picker",
			"id": "heading_font",
			"label": "Heading font",
			"default": "futura_n4"
		},
		{
			"type": "font_picker",
			"id": "body_font",
			"label": "Body font",
			"default": "futura_n4"
		}
	],
	"blocks": [
		{
			"name": "Heading",
			"type": "heading_item",
			"limit": 1,
			"settings": [
				{
					"type": "paragraph",
					"content": "This block will always show the priority at the top"
				},
				{
					"type": "header",
					"content": "Section heading"
				},
				{
					"type": "inline_richtext",
					"id": "sub_heading",
					"label": "Subheading",
					"default": "SUBHEADING"
				},
				{
					"type": "inline_richtext",
					"id": "heading",
					"label": "Heading",
					"default": "Add Heading"
				},
				{
					"type": "richtext",
					"id": "description",
					"label": "Description",
					"default": "<p>Share information about your brand with your customers. Describe a product, make announcements, or welcome customers to your store.</p>"
				},
				{
					"type": "select",
					"id": "heading_position",
					"label": "Heading alignment",
					"options": [
						{
							"label": "Left",
							"value": "sp-heading-alignment-left"
						},
						{
							"label": "Center",
							"value": "sp-heading-alignment-center"
						},
						{
							"label": "Right",
							"value": "sp-heading-alignment-right"
						}
					],
					"default": "sp-heading-alignment-center"
				},
				{
					"type": "select",
					"id": "sub_heading_size",
					"label": "Sub heading size",
					"options": [
						{
							"label": "Small",
							"value": "sp-sub-heading-size-small"
						},
						{
							"label": "Medium",
							"value": "sp-sub-heading-size-medium"
						},
						{
							"label": "Large",
							"value": "sp-sub-heading-size-large"
						}
					],
					"default": "sp-sub-heading-size-medium"
				},
				{
					"type": "range",
					"id": "heading_size_desktop",
					"min": 18,
					"max": 86,
					"step": 1,
					"unit": "px",
					"label": "Heading size (Desktop)",
					"default": 48
				},
				{
					"type": "range",
					"id": "heading_size_mobile",
					"min": 18,
					"max": 86,
					"step": 1,
					"unit": "px",
					"label": "Heading size (Mobile)",
					"default": 36
				},
				{
					"type": "range",
					"id": "description_size_desktop",
					"min": 12,
					"max": 24,
					"step": 1,
					"unit": "px",
					"label": "Description size (Desktop)",
					"default": 16
				},
				{
					"type": "range",
					"id": "description_size_mobile",
					"min": 12,
					"max": 24,
					"step": 1,
					"unit": "px",
					"label": "Description size (Mobile)",
					"default": 16
				},
				{
					"type": "range",
					"id": "heading_spacing_scale",
					"min": 50,
					"max": 200,
					"step": 10,
					"unit": "%",
					"label": "Heading spacing scale",
					"default": 100
				},
				{
					"type": "range",
					"id": "text_heading_spacing",
					"min": -10,
					"max": 10,
					"step": 0.2,
					"unit": "px",
					"label": "Text heading letter spacing",
					"default": 0
				},
				{
					"type": "range",
					"id": "heading_max_width",
					"min": 300,
					"max": 950,
					"step": 10,
					"unit": "px",
					"label": "Heading max-width",
					"default": 650
				}
			]
		},
		{
			"name": "Shop the look",
			"type": "shop_the_look_item",
			"settings": [
				{
					"type": "image_picker",
					"id": "shop_the_look_image",
					"label": "Image"
				},
				{
					"type": "header",
					"content": "Product 1"
				},
				{
					"type": "product",
					"id": "product",
					"label": "Product"
				},
				{
					"type": "range",
					"id": "horizontal_position",
					"min": 0,
					"max": 100,
					"step": 1,
					"unit": "%",
					"label": "Horizontal position",
					"default": 74
				},
				{
					"type": "range",
					"id": "vertical_position",
					"min": 0,
					"max": 100,
					"step": 1,
					"unit": "%",
					"label": "Vertical position",
					"default": 24
				},
				{
					"type": "header",
					"content": "Product 2"
				},
				{
					"type": "product",
					"id": "product_2",
					"label": "Product"
				},
				{
					"type": "range",
					"id": "horizontal_position_2",
					"min": 0,
					"max": 100,
					"step": 1,
					"unit": "%",
					"label": "Horizontal position",
					"default": 74
				},
				{
					"type": "range",
					"id": "vertical_position_2",
					"min": 0,
					"max": 100,
					"step": 1,
					"unit": "%",
					"label": "Vertical position",
					"default": 60
				},
				{
					"type": "header",
					"content": "Product 3"
				},
				{
					"type": "product",
					"id": "product_3",
					"label": "Product"
				},
				{
					"type": "range",
					"id": "horizontal_position_3",
					"min": 0,
					"max": 100,
					"step": 1,
					"unit": "%",
					"label": "Horizontal position",
					"default": 22
				},
				{
					"type": "range",
					"id": "vertical_position_3",
					"min": 0,
					"max": 100,
					"step": 1,
					"unit": "%",
					"label": "Vertical position",
					"default": 56
				},
				{
					"type": "header",
					"content": "Product 4"
				},
				{
					"type": "product",
					"id": "product_4",
					"label": "Product"
				},
				{
					"type": "range",
					"id": "horizontal_position_4",
					"min": 0,
					"max": 100,
					"step": 1,
					"unit": "%",
					"label": "Horizontal position",
					"default": 50
				},
				{
					"type": "range",
					"id": "vertical_position_4",
					"min": 0,
					"max": 100,
					"step": 1,
					"unit": "%",
					"label": "Vertical position",
					"default": 50
				}
			]
		}
	],
	"presets": [
		{
			"name": "🧩 Shop the look 1.3",
			"settings": {
				"image_position": "right",
				"image_border_radius": 0,
				"image_border_size": 0,
				"box_shadow": false,
				"product_card_content_alignment": "center",
				"product_card_style": "standard",
				"product_card_image_ratio": 0.8,
				"product_card_border_radius": 0,
				"product_card_badge_border_radius": 0,
				"product_card_border_size": 0,
				"product_card_box_shadow": false,
				"product_card_show_second_image": true,
				"product_card_show_vendor": true,
				"product_card_show_price": true,
				"product_card_show_discount_badge": true,
				"product_card_show_sold_out_badge": true,
				"product_card_discount_badge_type": "amount",
				"product_card_show_currency_code": false,
				"product_card_show_rating": false,
				"product_card_show_color_swatches": false,
				"product_card_color_swatch_style": "circle",
				"pagination_position": "bottom_center",
				"pagination_style": "line",
				"show_background_pagination": false,
				"pagination_border_radius": 20,
				"navigation_box_shadow": true,
				"navigation_style": "fill",
				"navigation_button_scale": 100,
				"navigation_button_radius": 40,
				"navigation_button_border_width": 1,
				"lazy_load": true,
				"section_background_color": "#FFF",
				"section_background_gradient_color": "",
				"section_text_color": "#1e1e1e",
				"hotspot_color": "#60FFCF",
				"image_overlay_color": "linear-gradient(0deg, rgba(0, 0, 0, 0.20) 0%, rgba(0, 0, 0, 0.20) 100%)",
				"image_border_color": "#1e1e1e",
				"navigation_background": "#FFF",
				"navigation_background_gradient": "linear-gradient(90deg, rgba(255, 255, 255, 0.4) 2%, rgba(255, 255, 255, 0.4) 100%)",
				"navigation_icon": "#1e1e1e",
				"navigation_background_shadow": "#23468F",
				"pagination_background": "",
				"pagination_highlight": "#868686",
				"section_top_padding": 100,
				"section_bottom_padding": 100,
				"section_border": false,
				"product_card_text_color": "#1e1e1e",
				"product_card_price_color": "#1e1e1e",
				"product_card_price_rating_color": "#1e1e1e",
				"product_card_background_color": "#FFF",
				"product_card_background_gradient_color": "linear-gradient(0deg, rgba(255, 255, 255, 1) 0%, rgba(255, 255, 255, 1) 100%)",
				"product_card_shadow_color": "#000",
				"product_card_border_color": "",
				"product_card_discount_badge_text_color": "#FFF",
				"product_card_discount_badge_background_color": "#C43235",
				"product_card_sold_badge_text_color": "#FFF",
				"product_card_sold_badge_background_color": "#ACACAC",
				"override_font": true,
				"heading_font": "futura_n4",
				"body_font": "futura_n4"
			},
			"blocks": [
				{
					"type": "shop_the_look_item",
					"settings": {
						"shop_the_look_image": "shopify://shop_images/shop-the-look-1-variant-3-image-1.png",
						"product": "",
						"horizontal_position": 74,
						"vertical_position": 24,
						"product_2": "",
						"horizontal_position_2": 74,
						"vertical_position_2": 60,
						"product_3": "",
						"horizontal_position_3": 22,
						"vertical_position_3": 56,
						"product_4": "",
						"horizontal_position_4": 50,
						"vertical_position_4": 50
					}
				}
			]
		}
	]
}
{% endschema %}

{% style %}
	#sp-{{section.id}} .sp-section{background-color:var(--sp-section-background-color);background-image:var(--sp-section-background-gradient-color)}#sp-{{section.id}} .sp-shop-the-look-item .sp-shop-the-look-image-wrapper .sp-shop-the-look-image-inner{border-radius:var(--sp-shop-the-look-image-border-radius);border:var(--sp-shop-the-look-image-border-width) solid var(--sp-shop-the-look-image-border-color)}#sp-{{section.id}} .sp-shop-the-look-item .sp-shop-the-look-image-wrapper .sp-shop-the-look-image-inner:before{background-image:var(--sp-shop-the-look-image-overlay-color)}#sp-{{section.id}} .sp-shop-the-look-item .sp-shop-the-look-image-wrapper .sp-shop-the-look-hotspots .sp-shop-the-look-hotspot-item{top:var(--sp-shop-the-look-hotspot-top);left:var(--sp-shop-the-look-hotspot-left);background:var(--sp-shop-the-look-hotspot-color)}#sp-{{section.id}} .sp-shop-the-look-item .sp-shop-the-look-image-wrapper .sp-shop-the-look-hotspots .sp-shop-the-look-hotspot-item:after{box-shadow:0 0 0 12px var(--sp-shop-the-look-hotspot-color)}#sp-{{section.id}} .sp-shop-the-look-item .sp-shop-the-look-image-wrapper .sp-shop-the-look-hotspots .sp-shop-the-look-hotspot-item.is-selected{width:24px;height:24px;margin-left:-12px;margin-top:-12px}#sp-{{section.id}} .sp-shop-the-look-item .sp-shop-the-look-image-wrapper .sp-shop-the-look-hotspots .sp-shop-the-look-hotspot-item.is-selected:after{box-shadow:0 0 0 24px var(--sp-shop-the-look-hotspot-color)}#sp-{{section.id}} .sp-shop-the-look-item[data-box-shadow=true] .sp-shop-the-look-image-wrapper:before{border-radius:var(--sp-shop-the-look-image-border-radius)}
{% endstyle %}
<script>
!function(){window.addEventListener("DOMContentLoaded",function(){e()});const c=new ResizeObserver(e=>{for(const s of e){var t=s.target;window.innerWidth<=1199?t.classList.add("is-active"):t.classList.remove("is-active","is-selected")}});function e(){var e=document.getElementById("sp-{{ section.id }}");if(e){const d=e.querySelector(".sp-shop-the-looks");e.querySelectorAll(".sp-shop-the-look-item").forEach(e=>{var t=e.querySelector(".sp-shop-the-look-content-pagination"),s=t.querySelectorAll(".dot"),t=(s.length&&(2<=s.length&&t.classList.add("sp-loaded"),s[0].classList.add("is-selected"),s[0].dispatchEvent(new Event("click"))),document.body.querySelector("#"+e.dataset.modalId)),o=(t&&t.remove(),document.createElement("div"));o.classList.add("sp-modal-shop-the-look","sp-modal-"+e.dataset.sectionId),o.id=e.dataset.modalId;const a=document.createElement("div");a.classList.add("sp-carousel");s=e.querySelector(".sp-shop-the-look-content-carousel").cloneNode(!0);s.querySelectorAll(".sp-shop-the-look-product-item").forEach(e=>{e.classList.remove("is-selected"),e.classList.add("sp-carousel-item"),a.appendChild(e)}),s.appendChild(a);const l=document.createElement("button");l.classList.add("sp-button-close"),l.innerHTML=`<svg width="36" height="36" viewBox="0 0 36 36" fill="none"
                                                 xmlns="http://www.w3.org/2000/svg">
                                                <path d="M26.3178 10.5014C26.3228 10.7713 26.1878 10.9713 26.0029 11.1562C23.7985 13.3556 21.5941 15.56 19.3947 17.7644C19.1648 17.9943 19.1648 17.9943 19.3997 18.2292C21.6091 20.4386 23.8185 22.648 26.0278 24.8574C26.5027 25.3323 26.3877 26.0421 25.7929 26.262C25.433 26.397 25.1281 26.292 24.8632 26.0271C23.8035 24.9674 22.7438 23.9077 21.6841 22.848C20.5194 21.6833 19.3447 20.5186 18.19 19.3439C18.0251 19.179 17.9551 19.204 17.8101 19.3539C15.5857 21.5833 13.3614 23.8127 11.132 26.0371C10.6721 26.4969 10.0023 26.387 9.75237 25.8221C9.60741 25.4972 9.68239 25.1573 9.97231 24.8674C10.6121 24.2226 11.2569 23.5828 11.8968 22.9429C13.4863 21.3534 15.0759 19.7588 16.6704 18.1743C16.8104 18.0343 16.8104 17.9693 16.6704 17.8294C14.4461 15.615 12.2267 13.3906 10.0023 11.1712C9.77236 10.9413 9.6274 10.6863 9.69738 10.3464C9.76236 10.0365 9.94231 9.81658 10.2422 9.72161C10.5421 9.62663 10.8221 9.67662 11.052 9.89656C11.3169 10.1515 11.5719 10.4114 11.8318 10.6713C13.8212 12.6608 15.8157 14.6502 17.8001 16.6497C17.9501 16.7996 18.0201 16.8196 18.18 16.6547C20.3994 14.4203 22.6288 12.1959 24.8532 9.97154C25.328 9.49667 26.0278 9.62163 26.2628 10.2165C26.3078 10.3064 26.3128 10.4014 26.3178 10.5014Z"
                                                      fill="currentColor"/>
                                            </svg>`;var n,i=d.dataset;for(n in i)o.dataset[n]=i[n];o.dataset.showPaginationMobile="true",o.dataset.showNavigationMobile="true",s.appendChild(l),o.appendChild(s),document.body.appendChild(o),new Flickity(a,{draggable:!0,freeScroll:!1,pageDots:!0,prevNextButtons:!0,wrapAround:!0,autoPlay:!1,adaptiveHeight:!0,watchCSS:!1,initialIndex:0,cellAlign:"center",groupCells:!1,selectedAttraction:.1,friction:.6,fade:!0,imagesLoaded:!0,on:{ready:function(){spRenderPagination(a,!1),spCalculateNavigation(a,".sp-card-information")}}}).on("resize",function(e){spCalculateNavigation(a,".sp-card-information")}),c.observe(o),l.addEventListener("click",function(e){l.closest(".sp-modal-shop-the-look").classList.remove("is-selected"),document.body.style.overflowY="",document.body.style.height=""})});const a=e.querySelector('[data-plugin="sp-carousel"]').querySelector(".sp-carousel"),l=new Flickity(a,{draggable:!0,freeScroll:!1,pageDots:!1,prevNextButtons:!0,wrapAround:!0,autoPlay:!1,adaptiveHeight:!0,watchCSS:!1,initialIndex:0,cellAlign:"left",groupCells:!0,selectedAttraction:.1,friction:.6,fade:!0,imagesLoaded:!0,on:{ready:function(){var e=Flickity.data(a),{options:t,cells:s}=e,o=s[0].element,o=(o.classList.add("current"),o.nextElementSibling);o&&o.classList.add("next"),s[s.length-1].element.classList.add("previous"),window.matchMedia("(max-width: 1199px)").matches?(t.fade=!1,e.cells.map(e=>{e.element.style.opacity=""})):(t.fade=!0,e.cells.map(e=>{e.element.style.opacity=""})),e.reloadCells(),e.select(0,!1,!0)}}});l.on("resize",function(e){var t=l["options"];window.matchMedia("(max-width: 1199px)").matches?(t.fade=!1,l.cells.map(e=>{e.element.style.opacity=""})):(t.fade=!0,l.cells.map(e=>{e.element.style.opacity=""})),l.reloadCells(),l.select(0,!1,!0)}),l.on("select",function(e){var t=l["cells"],s=t[e].element,o=t.length;t.map(e=>{e.element.classList.remove("current","next","previous")}),s.classList.add("current"),0<=e-1&&t[e-1].element.classList.add("previous"),(e+1<=o-1?t[e+1]:t[0]).element.classList.add("next")})}}Shopify&&Shopify?.designMode&&e()}(document);
</script>